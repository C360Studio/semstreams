{
  "id": "collect_research_evidence",
  "type": "expression",
  "name": "Collect Research Evidence",
  "description": "When a researcher completes successfully, record a reference entry in RESEARCH_EVIDENCE and add a research.has_evidence triple. The entry carries only identifiers (loop_id, task_id, role). Downstream consumers fetch findings via read_loop_result on the loop id. Rules carry references, not payloads — see ADR-028.",
  "enabled": true,
  "conditions": [
    {
      "field": "agent.loop.role",
      "operator": "eq",
      "value": "researcher"
    },
    {
      "field": "agent.loop.outcome",
      "operator": "eq",
      "value": "success"
    }
  ],
  "logic": "and",
  "on_enter": [
    {
      "type": "update_kv",
      "bucket": "RESEARCH_EVIDENCE",
      "key": "evidence.$entity.id",
      "payload": {
        "loop_id": "$entity.id",
        "role": "researcher",
        "collected_at": "$now"
      },
      "merge": false
    },
    {
      "type": "add_triple",
      "predicate": "research.has_evidence",
      "object": "$entity.id"
    }
  ]
}
