{
  "id": "synthesize_evidence",
  "type": "expression",
  "name": "Synthesize When Coordinator Decides",
  "description": "Fires when the research-coordinator decides synthesize. The synthesizer queries the graph for all research.has_evidence entries (written by rule 02 on researcher completions) and produces the final structured answer. Synthesis is no longer reflexive on every researcher completion — it's gated on the coordinator's judgment. See ADR-026 + ADR-028.",
  "enabled": true,
  "conditions": [
    {
      "field": "agent.loop.role",
      "operator": "eq",
      "value": "research-coordinator"
    },
    {
      "field": "coordinator.decision.next-action",
      "operator": "eq",
      "value": "synthesize"
    }
  ],
  "logic": "and",
  "cooldown": "30s",
  "on_enter": [
    {
      "type": "publish_agent",
      "subject": "agent.task.synthesis",
      "role": "general",
      "model": "general",
      "tools": ["read_loop_result"],
      "prompt": "You are a research synthesizer. The research-coordinator (loop $entity.id) has judged that enough evidence exists. Call read_loop_result on the coordinator loop to pull researcher outputs referenced in its decision payload. Produce a structured synthesis with:\n\n1. Summary (2-3 sentences)\n2. Key Findings (numbered, with source attribution if the researchers cited any)\n3. Confidence Assessment (well-supported vs. uncertain)\n4. Gaps (what is still unknown)"
    }
  ]
}
