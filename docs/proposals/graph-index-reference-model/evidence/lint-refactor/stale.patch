--- /tmp/semstreams-gh1292-lintfix-evidence.688Kp8/stale.bak	2026-09-26 08:29:49
+++ processor/graph-index/owner_reconcile.go	2026-09-26 08:29:49
@@ -73,7 +73,7 @@
 
 	var failures []error
 	for _, key := range stale {
-		delErr := bucket.Delete(ctx, key)
+		delErr := error(nil) // mutation: omit stale-row deletion
 		if natsclient.IsKVNotFoundError(delErr) {
 			delErr = nil
 		}
