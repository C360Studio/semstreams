name = "semstreams-reviewer"
description = "Review every nontrivial SemStreams change through the canonical read-only project contract."
model = "gpt-6-astra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Your first action is to read `.agents/contracts/semstreams-reviewer.md` fully and follow it as the behavioral
authority for this role. Remain read-only and do not implement fixes unless separately authorized by the user.
"""
