name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  GO_VERSION: "1.26"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      # gh#221: revive version pinned via go.mod's `tool` directive
      # (Go 1.24+). No install step — `go tool revive` resolves through
      # the module's pinned version. A new revive release can't surprise
      # CI with new ruleset warnings until we deliberately bump the pin
      # in go.mod. Closes Rule 4 of [[feedback_substrate_flake_discipline]].

      - name: Verify go.mod/go.sum are tidy
        # A machine with `GOFLAGS=-mod=mod` set (Go's default is
        # `-mod=readonly`) lets ANY go command — build, test, vet, list,
        # gopls — rewrite go.sum as a side effect. A full-module-graph
        # command then appends ~190 `/go.mod`-only hash lines that no
        # build needs. It is invisible: extra go.sum entries are inert,
        # so every other CI job stays green while the noise lands on main.
        # `-diff` reports what tidy WOULD change and writes nothing, so
        # this is a read-only check that names the offending lines.
        run: go mod tidy -diff

      - name: Run go vet
        run: go vet ./...

      - name: Run go fmt
        run: |
          fmt_output=$(go fmt ./...)
          if [ -n "$fmt_output" ]; then
            echo "The following files need formatting:"
            echo "$fmt_output"
            exit 1
          fi

      - name: Sanity-check revive version matches go.mod pin
        # gh#221 review M4 — catches the case where setup-go's cache
        # serves a stale binary that doesn't match the current go.mod
        # pin. Cheap (one line of grep) and turns silent drift into
        # loud failure.
        run: |
          # Match the `require` line shape `github.com/mgechev/revive vX.Y.Z`.
          # Use `v` after the package name to disambiguate from the `tool`
          # directive line (which lacks the version).
          expected=$(awk '/github.com\/mgechev\/revive v/ {print $2}' go.mod | head -1)
          # `revive --version` output differs by invocation context:
          # direct install emits "version 1.15.0", `go tool revive` on
          # CI's setup-go emits "v1.15.0". Strip the leading `v` from
          # both sides to normalize.
          actual_raw=$(go tool revive --version 2>&1 | head -1 | awk '{print $NF}')
          expected="${expected#v}"
          actual="${actual_raw#v}"
          if [ "$expected" != "$actual" ]; then
            echo "FAIL: revive version mismatch — go.mod pin: $expected, go tool raw: $actual_raw (normalized: $actual)"
            exit 1
          fi
          echo "OK: revive $actual matches go.mod pin"

      - name: Run revive (pinned via go.mod tool directive)
        run: go tool revive -config revive.toml -formatter friendly ./...

      - name: Entity-ID corpus audit (lexical + segment rules)
        # ADR-102 / entity-id-contract "Segment semantics are enforced by the
        # entity-ID corpus audit": the tracked corpus must have zero invalid or
        # unclassified candidates. Same invocation as `task entity-id:audit`.
        run: go run ./cmd/entity-id-audit .

      - name: Substrate-flake guard — fixed-port lint regex matrix test
        # gh#220 review C1 — the lint guard's regex matrix must be
        # verified, not implicitly trusted. Runs fixture test that
        # exercises positive matches + suppression marker.
        run: bash scripts/lint-test-ports_fixture_test.sh

      - name: Inventory-verify guard — line-pin drift detection matrix test
        # Verifies scripts/inventory-verify.sh reports OK/MOVED/AMBIGUOUS/
        # DRIFT/MALFORMED/BASE-unknown the way it claims to, and stays
        # silent-safe (explicit "no pins found") on a broken inventory.
        run: bash scripts/inventory-verify_fixture_test.sh

      - name: Substrate-flake guard — no fixed-port net.Listen in test files
        # gh#220 Subclass 2 — fixed-port literals in *_test.go collide
        # under parallel test execution. Single source of truth:
        # scripts/lint-test-ports.sh, also invoked by task lint.
        run: bash scripts/lint-test-ports.sh

      - name: natsclient raw-Request guard (gh#337)
        # Fails if production code calls the raw natsclient.Request /
        # RequestWithRetry methods, which return a handler error as a
        # success-shaped {message,detail} body (the silent-success class).
        # Use RequestClassified instead. Guard + allowlist live in
        # test/natsclient/request_guard_test.go; also invoked by `task lint`.
        # Fast-feedback duplicate of the coverage in the Test job's go test.
        run: go test ./test/natsclient/

      # OpenSpec is validated here, not only in the tag proof: a change is
      # archived in the PR that lands it (openspec/README.md rule 2), so the
      # promoted specs must be checked on the same PR. Pinned CLI version so a
      # new release cannot change validation semantics without a deliberate bump.
      - name: Set up Node (OpenSpec CLI)
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install OpenSpec CLI (pinned)
        run: npm install -g @fission-ai/openspec@1.7.0

      - name: Validate OpenSpec changes and specs (strict)
        run: openspec validate --all --strict --no-interactive

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Run additive unit and integration suite
        run: scripts/run-integration-tests.sh

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Build binary
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build \
            -ldflags="-w -s -X main.version=${{ github.sha }}" \
            -o semstreams-linux-amd64 \
            ./cmd/semstreams

      - name: Test binary execution
        run: |
          ./semstreams-linux-amd64 --version || true
          echo "Build successful"

  schema-validation:
    name: Schema Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install task
        run: |
          sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b /usr/local/bin

      - name: Generate schemas and OpenAPI spec
        run: task schema:generate

      - name: Run OpenAPI generator tests
        run: go test -v ./cmd/openapi-generator

      - name: Check for uncommitted schema changes
        run: |
          if ! git diff --exit-code schemas/ specs/openapi.v3.yaml; then
            echo "❌ Schema or OpenAPI files have uncommitted changes!"
            echo ""
            echo "The following files changed after running 'task schema:generate':"
            git diff --name-only schemas/ specs/openapi.v3.yaml
            echo ""
            echo "Please run 'task schema:generate' locally and commit the changes."
            exit 1
          fi
          echo "✅ All schemas and OpenAPI spec are up to date"

  api-compat:
    name: Tier 1 API Compatibility
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        # fetch-depth: 0 is load-bearing. api-compat.sh resolves its baseline with
        # `git tag --list 'v1.0.0-*' --merged HEAD`, and the default shallow checkout carries no tags — the script
        # would exit 2 ("no base version") on every run, which is a broken gate rather than a passing one.
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install task
        run: |
          sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b /usr/local/bin

      # Hermetic and fast: proves the guard's failure modes still fail. This step BLOCKS — a release gate that
      # cannot detect its own broken input is worse than no gate.
      - name: Guard fixture test
        run: task api:compat:test

      # Reporting posture until RC (ADR-106 RC-4). The count is expected to be non-zero and to descend; at RC this
      # step drops `continue-on-error` and `:report`, and any incompatible Tier 1 change fails the build.
      - name: Tier 1 API compatibility report
        continue-on-error: true
        run: task api:compat:report

  status-check:
    name: CI Status Check
    runs-on: ubuntu-latest
    needs: [lint, test, build, schema-validation, api-compat]
    if: always()
    steps:
      - name: Check if all jobs succeeded
        run: |
          if [[ "${{ needs.lint.result }}" != "success" ]] || \
             [[ "${{ needs.test.result }}" != "success" ]] || \
             [[ "${{ needs.build.result }}" != "success" ]] || \
             [[ "${{ needs.schema-validation.result }}" != "success" ]] || \
             [[ "${{ needs.api-compat.result }}" != "success" ]]; then
            echo "One or more CI jobs failed"
            exit 1
          fi
          echo "All CI jobs passed successfully"
