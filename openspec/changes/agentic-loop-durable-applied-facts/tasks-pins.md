# tasks.md pin map — `68c14c8e` → `b7ce8727`

Rules: old → new with the `sed -n "${n}p"` text of the new line (every new line is also a pin in `inventory.md` § 7); NO TARGET names the `main` attach site; wording changes are given as exact replacement text. Tasks are not rewritten here. Abbreviations as in `inventory.md`. Test-file existence measured with `[ -e ]` on the worktree.

## 1. Field and invariant surface

- **1.0** no pins. Two prefix-only readers exist on `main` that `Parse` should replace so the grammar has one reader: `ST:1360-1361` (`func (m *LoopManager) ExtractLoopIDFromRequest(requestID string) string {` / `parts := strings.Split(requestID, ":req:")`) and `LP:50` (`func loopIDFromStructuredID(structuredID, separator string) string {`). Wording add: "`Next` must reproduce `ST:1345-1347` (`iteration = entity.Iterations + 1`; `%s:req:%d:%d`), so birth mints `…:req:1:0` from `Iterations = 0`."
- **1.1** `agentic/state.go:54` → `AG:57` — `PendingToolResults map[string]ToolResult \`json:"pending_tool_results,omitempty"\` // ExecutionID; synthetic failures use CallID` (the field goes beside the applied set); `:96-108` `Validate` → `AG:136` — `func (e *LoopEntity) Validate() error {`; `:132-150` `TransitionTo` → `AG:171` — `func (e *LoopEntity) TransitionTo(newState LoopState) error {`. `agentic/state_test.go` EXISTS.
- **1.2** `state.go:349-430`, `:356` → NO TARGET (L2 residual: "restore-from-KV absent", archived `stable-request-identity/design.md:186`). Attach: new function in `ST` beside `attachContinuation` (`ST:298` — `func (m *LoopManager) attachContinuation(loopID, taskID string) (agentic.LoopEntity, bool, error) {`). Wording: "`restoreLoopFromRequest` (`:349-430`) is fed the record step 0 (2.5) already adopted … (identity checks at `:356` unchanged)" → "new `restoreLoopFromRequest` (no rebuild path exists on `main`) is fed the record step 0 (2.5) already adopted plus the newest retained request; it rebuilds the ContextManager, the routing maps (`ST:79` `outstandingRequests`, `ST:887-888`), and the tool batch (`restoreToolBatch`, membership against the retained response only)". `state_test.go` EXISTS.

## 2. Carrier

- **2.1** `component.go:2396-2415` `persistLoopState` → `C:2468` (def) … `C:2483` — `if _, err := c.loopsBucket.Put(ctx, loopID, data); err != nil {`; `:1782-1799` `persistHandlerResult` → `C:1923` (def), `C:1947` — `if err := c.persistResultState(ctx, result, terminal); err != nil {`, `C:1959` — `if err := c.publishResults(ctx, result); err != nil {`; birth `:1405 → :1410` → `C:1496` — `c.publishResults(ctx, result)` THEN `C:1499` — `c.persistLoopState(ctx, result.LoopID)`. Wording: "birth (`:1405` → `:1410`) keeps Put → publish" → "birth (`C:1496` → `C:1499`, today publish then `Put` with the error ignored) becomes `Put` → publish, and a failed birth write returns Retry (the first of #1345's five task-intake branches, converted by necessity; the other four stay #1345's)". Add: "`persistLoopState`'s other callers ride the same `Update`: `C:1425` (deferred continuation marker) and `AS:101` (sweeper; its publish-then-`Put` pair moves onto the carrier with the approval lane in #1362, not here)." Add: "the ORDER flip is scoped by call site to the model-response and tool-result lanes; `ARH:205` and `AS:100-101` keep today's order (coordinator scoping under OQ7/OQ8)." Add: "on `ErrKVRevisionMismatch` (`KV:238`) return Retry and release the loop's process state (`ST:574`, `C:1964`)". `persist_handler_result_test.go` EXISTS.
- **2.2** `component.go:2331-2354` → `C:2318-2333`; `C:2328` — `if err := c.natsClient.PublishToStreamWithMsgID(ctx, msg.Subject, msg.Data, msg.MsgID); err != nil {`. **DONE by L2** (`H:55`, `H:1174`, `H:2194`, `H:2958`; `publication_semantics_integration_test.go` EXISTS). Wording: mark `[x]` with "shipped by #1328 (PR #1335); L4 verifies the window collapse only".
- **2.3** `SR:273-323`, `SR:28-32` → NO TARGET. Attach: new reader in agentic-loop (pattern `PS:21` / `PS:28` / `PS:37` `GetLastMsgForSubject`; `TR:51`), called in the carrier before `C:2318` for the messages minted at `H:1122`, `H:2168`, `H:2927`. Wording: "call `readRetainedAgentRequest` (`SR:273-323`)" → "call the new `readRetainedAgentRequest` (identity only; `agent.request.<loopID>` newest)"; "through the `loopSettlementEvidenceReader` seam (`SR:28-32`)" → "through a new evidence-reader interface (two reads: request, response)".
- **2.4** `handlers.go:1083` via `buildTaskResultFromRequest` → `H:1122` — `RequestID:      h.loopManager.GenerateRequestID(loopID),` inside `buildTaskRequest` (`H:1120`); `:1981` → `H:2168` (`emitRetryRequest`, `H:2141`; MsgID `H:2194`); `:2654` → `H:2927` (`publishIterationRequest`, `H:2886`; MsgID `H:2958`); `state.go:601-616` → `ST:466` — `func (m *LoopManager) IncrementTruncationRetry(loopID string) int {` and `ST:477` — `func (m *LoopManager) ResetTruncationRetry(loopID string) {`; callers to delete `H:2037`, `H:1389`, `H:1409`; `GenerateRequestID` (`ST:1339-1347`) becomes `looprequest.Next(PublishedRequestID)`. `handlers_test.go` EXISTS.
- **2.5** `settlement_recovery.go` `:63`, wire `:517`, `:622`, `:867` → NO TARGET. Attach: the cold arms `C:1700` (`settleResponseWithoutLoop`), `C:2292` (`settleToolResultWithoutLoop`), `C:2773`-`:2780` (`settleVerdictWithoutWaiter`), and the approval lane's new `ErrLoopNotFound` branch at `ARH:58` — `if !ok {`; NOT the task lane (Q1) and NOT cancel (`C:2593-2612` classifies by `State` only). `agentic/state.go:96-108` → `AG:136`. Wording: "read the newest retained request (`:63`)" → "read the newest retained request (task 2.3's reader)"; "`Iterations`" → "`Iterations = parsed iteration − 1` (`ST:1345`: a record's request carries `Iterations + 1`)"; keep "`PendingToolResults = nil`" and note in § 3.6 that on `main` this IS the advance's shape (`H:2870` → `ST:1121`).

## 3. Lane classification

- **3.1** `:553-682`, `:686-740`, `:763-817`, `:821-852`, `:634-636`, `state.go:486-488`, `:435-444` → NO TARGET (never built). Attach: classification before `HandleToolResult` at `C:2195` — `result, err := c.handler.HandleToolResult(ctx, loopID, toolResult)` (warm) and the live arm of `C:2287-2292` (cold). Wording: "delete `toolResultProvenInLaterRequest` … `requirePreceding` (`:435-444`)" → "nothing to delete on `main`; build the classification at `C:2195` (ahead of `H:2546` and `H:2580`) and in `C:2292`'s live arm". `tool_result_recovery_test.go` ABSENT → "rewrite" becomes "write".
- **3.2** `:454-548`, `:517-535` → NO TARGET. Attach: `C:1698-1700` live arm (cold) and `H:1253` — `if current := h.loopManager.CurrentRequest(loopID); current != "" && current != response.RequestID {` (warm guard reads `PublishedRequestID`). `settlement_recovery_test.go` ABSENT → new file.
- **3.3** `:1034-1040`, `:984-1013`, `:743-758`, `:857-917`, `:921-981` → NO TARGET. Attach: the new cold branch at `ARH:58` (today `staleDrop` `ARH:78` → Ack `ARH:194-199`). Wording: "I4 replaces `validatePendingApprovalRequest` (`:1034-1040`) …" → "the cold branch checks I4 and key presence against the record it just adopted; `republishPendingApproval` is built inside it; `settleAbsentApprovalEvidence` is OQ1". Add: "the sweeper's publish → `Put` pair (`AS:100-101`) rides `persistHandlerResult`" — with this lane, in #1362. `approval_timeout_recovery_test.go`, `approval_restore_order_test.go` ABSENT → new.
- **3.4** `:377-452` → NO TARGET. Attach: a cold fork before `C:1396` — `result, err := c.handler.HandleTask(ctx, *task)`; R1 rebuild = `H:1120` `buildTaskRequest`. `recovery_test.go` EXISTS (extend).
- **3.5** `metrics.go` `:20`, `:113`, `:304`, `:336`, `approval_response_handler.go:204` → NO TARGET (`approvalDecisionsInapplicable` does not exist). `main` home: `M:35` — `toolResultsDropped  *prometheus.CounterVec`, name `M:169` — `Name:      "tool_results_dropped_total",`, registration `M:301`, recorder `M:527` — `func (m *loopMetrics) recordToolResultDropped(reason string) {`; the reason values are enumerated in the recorder's doc comment `M:514-526`, NOT in a constant block (`M:382-383` is the verdict-drop pair, a different counter); existing use `C:2300` — `c.metrics.recordToolResultDropped("stale_execution")`. Wording (owner-visible substitution): "new `toolResultsInapplicable` counter at the four `approvalDecisionsInapplicable` sites" → "two new reason values on the existing `tool_results_dropped_total` (`older_request`, `terminal_unproven`; doc comment `M:514-526`, recorder `M:527`)". `C@68c:2178-2180` → `C:2195`; wording: "warm-lane `component.go:2178-2180` takes the same ACK when terminal" → "the warm check is inserted before `HandleToolResult` at `C:2195`, because `H:2546` and `H:2580` precede the guard at `H:2652`". `terminal_tool_recovery_test.go` ABSENT → new.
- **3.6** `component.go:2585-2596` → `C:2734` (call) and `C:2759` — `func (c *Component) settleVerdictWithoutWaiter(` (signature continues `C:2760-2761`: `ctx context.Context, payload VerdictPayload, executionID string, cause error,` / `) (natsclient.DeliveryDecision, error) {`), Ack `C:2773`, Retry `C:2780`; `governance_dispatcher.go:557-560` → `GD:608-629` (`ErrNoGovernanceWaiter` `GD:337`). Metric: `M:393` `RecordGovernanceVerdictMissingWaiter`. `verdict_wire_test.go` ABSENT → new.
- **3.7** `component.go:1833-1870`, `:1838-1839`, `:1846`, `:1959-1960`, `:1852-1856`, `:1861-1866` → NO TARGET. Attach: build one owner from `C:1974` — `func (c *Component) persistResultState(ctx context.Context, result HandlerResult, terminal bool) error {` (`:1975`, `:1982` → `:2401`, `:2002` → `:2432`), `C:1734`/`C:1794` (`:1752`, `:1809`, `:1831`), `C:2614` (`:2631`, `:2668`, `:2683` → `:2456`); marker `Put` → `Create` (`KV:211`, `KV:218`), entity `Update` (`KV:231`). Wording: "delete the compares (`:1852-1856`, `:1861-1866`)" → "no compares exist on `main`; build arms (a) terminal at the observed revision → Q7 ACK, (b) marker `Create` refused → read it back, adopt by loop ID + kind, log differences, (c) fresh → Create → stamps → publish → `Update`"; add "clears `PendingApproval` on the terminal transition (L3's deferred item, archived L3 design `:50`)". `terminal_owner_test.go` ABSENT → new.
- **3.8** unchanged; both searches re-run on `main` return the same (0 hits; openspec only).
- **3.9 (new, metering ruling 2026-09-21)** see `reconciliation.md` § E for the line; sites `processor/agentic-dispatch/http_activity.go:321` / `:334`, `metrics.go:180`, `commands.go:71`, `command_target_resolution_test.go:313`, `:319`, `:335`, `:339`, `:349`.

## 4. Evidence

- **4.1** no pins; generator lanes unchanged.
- **4.2** `ARH:269`, `:275` → NO TARGET; on `main` the approval lane's write is the carrier's (`ARH:205` — `if err := c.persistHandlerResult(ctx, result); err != nil {` → `C:1947` → `C:1959`). Wording: "crash between `ARH:269` and `:275`" → "crash between the carrier's publish (`C:1959`) and its `Update` for the approval lane's result (`ARH:205`)". `tool_result_redelivery_integration_test.go`, `terminal_tool_redelivery_integration_test.go` ABSENT → new; `processbarrier` EXISTS.
- **4.3** (d) "restore the terminal compare in 3.7" → "restore `Put` for the marker in 3.7 (drop the `Create` read-back) → the terminal case in 4.2 fails"; (a), (b), (c), (e), (f) unchanged.

## 5-6. Docs, spec, verification

- **5.1** `docs/concepts/17-approval-flow.md:65-68` → `:65` holds (`- **Restart-safe.** \`LoopEntity.PendingApproval\` lives in the`); `doc.go` EXISTS.
- **5.2**, **6.1** unchanged.
- **6.2** `stage_a_process_replacement.go` (`:26` pinned), `process_replacement_test.go` EXIST; `approval_restart.go`, `approval_restart_test.go` ABSENT → "write new"; the stage cannot pass without OQ1/OQ2.

## Substitution summary (tasks whose wording must change)

1.0 (add the `Iterations + 1` constraint), 1.2, 2.1, 2.2 (mark done), 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5, 3.7, 4.2, 4.3(d), 6.2; new 3.9. Unchanged: 1.1 (pins only), 3.6 (pins only), 3.8, 4.1, 5.1, 5.2, 6.1.
