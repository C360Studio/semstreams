# Tasks

- [x] 1. Add failing coverage for the public canonical lesson projection-contract snapshot.
- [x] 2. Prove every returned top-level and nested slice is independent from later calls.
- [x] 3. Derive the internal built-in aggregate and public adapter from one canonical lesson declaration.
- [x] 4. Build the real-NATS integration client from the public snapshot, inject its narrow capabilities through
      `NewLessonCurator`, and prove all 11 birth predicates and lifecycle sibling cleanup.
- [x] 5. Strengthen ops E2E post-promotion identity assertions and count all nine completed load-bearing stages.
- [x] 6. Restore composition-root-local E2E wiring and keep `NewNATSLessonCurator` absent.
- [x] 7. Update the agent-memory guide, lesson rule-pack README, projection-client guide, ADR-097, release note, and
      downstream migration guide to the snapshot design.
- [x] 8. Run unit, integration, race, lint, schema-drift, strict OpenSpec, and `task e2e:ops` verification.
- [x] 9. Obtain SemStreams reviewer approval before integration.
