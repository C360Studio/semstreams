# Conformance — flow-invalid-handling-contract

Per-ruling map from the owner-accepted flow-CRUD design (`docs/proposals/gh1008-1010-flow-crud-design.md:9-11`
acceptance; `:291-304` the twelve rulings; `:126-209` the Slice C contract) to the code, spec delta, and test that
carry it. Every `file:line` in the Implementation and Test columns is **to be measured by the implementer at the head
that holds the last change to any `.go` file or spec delta on the branch**; the placeholders below name the file and
the mechanism, not a line. `tasks.md` rows cite section numbers, which do not shift. The owner-run Codex round made
this table mandatory for every slice (Slice B `conformance.md`, PR #1085).

| # | Ruling / contract bullet | Implementation | Spec delta | Test / evidence |
|---|---|---|---|---|
| R1 | Option 2A and four pre-tag slices (`:293`) | Slice C is this change: `proposal.md:3-5` | `specs/flow-authoring/spec.md` (this delta) | draft PR #____, `Closes #1008` |
| R2 | CAS repair inside #1009 (`:294`) | N/A — Slice A (#1083) | — | — |
| R3 | Copy-on-write, success-only caller mutation (`:295`) | N/A — Slice A (#1083) | — | — |
| R4 | Stable issue types and aggregate structural validation (`:296`) | `flowstore/validation.go:__` (twenty constants), `flowstore/flow.go:__` (`Validate` aggregation, order, identity), `engine/validator.go:__` (constants replace literals) | delta: "Structural validation is an aggregated, stable vocabulary owned by flowstore"; "The validation result is one data definition…" | `flowstore/flow_test.go:__` `TestFlowValidateAggregatesStructuralIssuesInOrder`, `TestFlowValidateErrorCarriesResultAndIsInvalid`; `engine/…_test.go:__` `TestValidationIssueTypesAreTheClosedVocabulary` |
| R5 | HTTP 200 for well-formed invalid drafts (`:297`) | `engine/engine.go:__` (structural → result), `service/flow_service.go:__` (`handleValidateFlow`) | delta: "Validate returns 200 for every well-formed draft" | `service/flow_service_test.go:__` `TestHandleValidateFlowStructuralInvalidIsOK`, `TestHandleValidateFlowPatternErrorIsOK`; `engine/…_test.go:__` `TestEngineValidateFlowDefinitionStructuralInvalidIsResult` |
| R6 | `connection_pattern_error` as validation finding (`:298`) | `engine/validator.go:__` (the conversion at the former `:137-141` site) | delta: "A connection-pattern failure is one validation finding" | `engine/…_test.go:__` `TestValidatorPatternErrorBecomesFinding` |
| R7 | Flow error response without a new machine-code field (`:299`) | `service/flow_service.go:__` (`FlowErrorResponse`: `error` + optional `validation_result`, nothing else; `projectFlowError`) | delta: "Flow HTTP failures project through one classified mapper into FlowErrorResponse" | `service/flow_error_test.go:__` `TestFlowErrorMapperClassMatrix`, `TestFlowErrorResponseOmitsAbsentResult`; `service/flow_surface_test.go:__` `TestFlowOpenAPIErrorAndValidationSchemas` (required set exactly `[error]`) |
| R8 | Must-exist DELETE (`:300`) | `flowstore/manager.go:__` (read before delete), `service/flow_service.go:__` (`handleDeleteFlow`) | delta: "DELETE is must-exist" | `service/flow_service_test.go:__` `TestHandleDeleteFlowMustExist`; `flowstore/manager_integration_test.go:__` `TestManagerDeleteMissingReportsTypedAbsence`; `processor/agentic-tools/executors/flows_integration_test.go:__` `TestFlowExecutorDeleteMissingIsError` |
| R9 | Request-schema separation and legacy bodies (`:301`) | N/A — landed in Slice A (#1083); Slice C adds no request field and keeps decoding legacy bodies (no `DisallowUnknownFields`; `grep -n DisallowUnknownFields service/flow_service.go` → 0 at head, re-check at the final head) | — | Slice A's `TestFlowUpdateRequestSchemaOmitsServerAuditFields` still green in §6 |
| R10 | Separately reversible pre-tag Get projection (`:302`) | N/A — Slice D (#1060) reuses this change's mapper and `FlowErrorResponse` | — | — |
| R11 | Mandatory semstreams-ui candidate validation (`:303`) | → #1087 checklist (downstream gate); the sister repo is hands-off; the 9/9 evidence is recorded on #1087 | — | not satisfiable inside this PR |
| R12 | Named E2E scenarios and assertion totals (`:304`, `:242-277`) | → #1087. **Filing records the obligation; it does not complete the pre-tag guarantee.** `flow-authoring-http-contract` (core, 14 assertions) is this slice's behaviour plus A's timestamps and D's deleted Get. Whether it is authored inside this PR is the BREAKING ruling (`proposal.md` §Open questions 3; `tasks.md` 6.7) | — | the per-slice gate this PR ran: `tasks.md` 6.1–6.6 |
| C1 | `ValidationResult` requires status and non-null errors/warnings/nodes/discovered_connections; status derives errors → warnings → valid (`:133-134`) | `flowstore/validation.go:__`; `engine/validator.go:__` (allocation on every return path) | delta: "The validation result is one data definition…" | `engine/…_test.go:__` `TestValidationResultArraysAreNonNull` |
| C2 | `ValidationIssue` requires stable type, severity, non-empty component_name/message, non-null suggestions; port_name optional (`:134-135`) | `flowstore/validation.go:__` (`Suggestions` without `omitempty`), every emission site allocates | same | `TestValidationResultArraysAreNonNull`; `service/flow_surface_test.go:__` `TestFlowOpenAPIErrorAndValidationSchemas` (required set) |
| C3 | Data definition moved to `flowstore`; `engine` alias kept (`:135`) | `flowstore/validation.go:__` (five types + `ValidationError`); `engine/validator.go:__`, `engine/engine.go:__` (aliases) | same | `engine/…_test.go:__` `TestEngineValidationTypesAreFlowstoreAliases`; `grep -rn '"github.com/c360studio/semstreams/engine"' --include='*.go' .` → `service/flow_service.go` only (re-measure) |
| C4 | Structural aggregation order: Flow fields, nodes in input order, duplicate after required fields, connections in order; all severity error (`:137-138`) | `flowstore/flow.go:__` | delta: "Structural validation is an aggregated…" | `flowstore/flow_test.go:__` `TestFlowValidateAggregatesStructuralIssuesInOrder` |
| C5 | The twelve structural types (`:140-147`) | `flowstore/validation.go:__` | same | same (twelve-fault case) |
| C6 | Component identity: Flow ID or `(flow)`; node name → ID → index; connection ID → index; first non-empty duplicate referenceable (`:149-150`) | `flowstore/flow.go:__` (one identity helper) | same | same |
| C7 | Structural issues stop before graph work; non-null empty node/connection arrays; empty nodes stays saved-authoring-valid; Engine emits `empty_flow` (`:150-151`) | `flowstore/flow.go:__`; `engine/validator.go:__` (`empty_flow` retained, Flow identity as component) | same | `TestFlowValidateAggregatesStructuralIssuesInOrder` (no graph work: `nodes`/`discovered_connections` empty non-nil); `TestValidationResultArraysAreNonNull` (empty flow) |
| C8 | Eight stable graph types (`:153-154`) | `flowstore/validation.go:__`; `engine/validator.go:__` (literals replaced) | delta: "The validation result is one data definition…" | `TestValidationIssueTypesAreTheClosedVocabulary` |
| C9 | Pattern errors → exactly one safe error finding, status errors, non-null arrays, nil execution error; Compile then yields the existing validation failure (`:154-155`) | `engine/validator.go:__` | delta: "A connection-pattern failure is one validation finding" | `TestValidatorPatternErrorBecomesFinding`; `TestHandleValidateFlowPatternErrorIsOK` |
| C10 | Validate: 200 for valid/warning/structural-invalid/graph-invalid/pattern-error drafts; 400 malformed/ID mismatch; unexpected execution failure 500; Slice D owns the pre-read (`:159-160`) | `service/flow_service.go:__` (`handleValidateFlow`) | delta: "Validate returns 200 for every well-formed draft" | `TestHandleValidateFlowStructuralInvalidIsOK`, `TestHandleValidateFlowPatternErrorIsOK`, `TestHandleValidateFlow_InvalidJSON`, `TestHandleValidateFlow_IDMismatch` |
| C11 | `FlowErrorResponse`: non-empty `error`; optional non-null `validation_result`; present for Create/Update/publication validation, omitted otherwise (`:162-163`) | `service/flow_service.go:__` (`omitzero` value field); `service/schema.go:__` (`omitzero` optional) | delta: "Flow HTTP failures project…"; "OpenAPI declares…" | `TestFlowErrorResponseOmitsAbsentResult`; `TestSchemaFromTypeTreatsOmitzeroAsOptional`; `TestFlowOpenAPIErrorAndValidationSchemas` |
| C12 | Exact public messages (`:165-173`) | `service/flow_service.go:__` (the message constants; one table) | delta: "Flow HTTP failures project…" | `TestFlowErrorMapperClassMatrix` (exact `==` per row) |
| C13 | Create 201; 400 malformed/validation; 409 existing; 503/504/500. Update 200; 400 malformed/mismatch/validation; 404 missing; 409 conflict; 503/504/500 (`:175-176`) | `service/flow_service.go:__` (`handleCreateFlow`, `handleUpdateFlow`) | same | `TestHandleCreateFlowInvalidIsBadRequestWithResult`, `TestHandleCreateFlowExistingIDIsConflict`, `TestHandleUpdateFlowInvalidIsBadRequestWithResult`, `TestHandleUpdateFlowMissingIsNotFound`, `TestFlowCRUDDoesNotPublishAndExplicitPublicationRetriesThroughConfigManager` (409 message) |
| C14 | List keeps Slice B's empty/current-state behaviour; failures project deadline → 504, transient → 503, corrupt/fatal/unknown → 500; sanitized body; raw NATS/malformed content log-only (`:178-180`) | `service/flow_service.go:__` (`handleListFlows` → mapper) | delta: "List failures project by class" | `TestHandleListFlowsFailureProjectionMatrix`; Slice B's `TestHandleListFlowsEmptyResponseIsNonNullArray` still green |
| C15 | DELETE must-exist: existing → 204 no body/content-type; absent/repeated → 404; invalid direct ID → 400; deadline/transient/fatal → 504/503/500; FlowExecutor Delete retains missing as error (`:182-183`) | `flowstore/manager.go:__`; `service/flow_service.go:__` | delta: "DELETE is must-exist" | `TestHandleDeleteFlowMustExist`, `TestManagerDeleteMissingReportsTypedAbsence`, `TestFlowExecutorDeleteMissingIsError`. The "invalid direct ID → 400" clause has no reachable HTTP producer at head and no cell in the design's final matrix (`:225`) — see DEVIATION candidates |
| C16 | Publication: 200 progress; 400 validation with result; 404/503/504 pre-read; corrupt/internal 500 Flow error; component-config persistence 500 retains progress; OpenAPI 500 `oneOf` (`:185-186`) | `service/flow_service.go:__` (`handlePublishComponentConfigs`); `service/openapi_types.go:__` (`SchemaRefs`); `cmd/openapi-generator/openapi_builder.go:__` (`oneOf`) | delta: "Publication responses carry the shared error schema"; "OpenAPI declares…" | `TestHandlePublishComponentConfigsInvalidIsBadRequestWithResult`, `TestHandlePublishComponentConfigsStructuralInvalidHasResult`, `TestOpenAPIBuilderRendersOneOfResponse`, `TestFlowOpenAPIErrorAndValidationSchemas`. The pre-read's 503/504 rows are Slice D's |
| C17 | Actual adopter outcome: editor live validation and MCP consume structural-invalid 200 automatically; no claim that Create/Save UI renders detail (`:188-192`) | — (sister repo hands-off; read-only inspection at `39f5f04` recorded in `proposal.md` §Actual adopter outcome) | — | #1087 downstream evidence |
| C18 | Named tests present (`:196-199`) incl. `TestHandleListFlowsFailureProjectionMatrix` table-testing deadline→504, transient→503, corrupt/fatal→500, sanitized bodies, preserved Manager class | — | every scenario in the delta names its test | the reconciliation table in `tasks.md` 7.3 |
| C19 | Minimum assertions: all 12 types/order, no graph work, issue safety, pattern Validate 200/publication 400, invalid mutations, typed conflicts, DELETE 204 then 404, shared schema, sanitized 5xx (`:201-202`) | — | — | assertion count measured at the final head, recorded here (`t.Error*`/`t.Fatal*`/`assert.`/`require.` inside each named test body) |
| C20 | Baseline captured for invalid Create/Update/Validate, pattern error, null arrays, repeat Delete; forced omissions of opaque Create, fail-fast validation, returned pattern error, nil arrays, string conflict must fail at least five named tests (`:204-206`) | — | — | `tasks.md` 2.9 (capture) and §4 (M1–M5 the design's five; M6–M12 one per new mechanism) |
| C21 | Generate `ValidationIssue`, `ValidationResult`, `FlowErrorResponse`, and operation rows per Slice C; commit only its deltas; regenerate to no drift; full slice gate (`:208-209`) | `service/flow_service.go:__` (`ResponseTypes`, rows); `specs/openapi.v3.yaml:__` | delta: "OpenAPI declares…" | `tasks.md` §5, §6 |

## DEVIATION

Empty unless the implementer finds one. Candidates recorded for the reviewer, none of which is a deviation from an
accepted ruling:

- The design's DELETE text says "invalid direct ID → 400" (`:182-183`); its final matrix lists no `400` for Delete
  (`:225`); no producer is reachable at head. The mapper keeps a generic-invalid branch; its message is
  `proposal.md` open question 1. Record the owner's answer here with the comment URL.
- "Move its data definition to `flowstore`" (`:135`) is implemented as five types plus `ValidationError`, because
  `ValidationResult` embeds the other four and `engine` imports `flowstore`. Not a deviation; a closure.
- `empty_flow`'s `component_name` changes from `(none)` (`engine/validator.go:105` at head) to the Flow identity, the
  rule the design states for Flow-level findings (`:149`). Record if the owner prefers `(none)` retained.
