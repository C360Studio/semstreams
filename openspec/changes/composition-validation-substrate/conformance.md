# Conformance — composition-validation-substrate

Per-decision map from ADR-100 (`docs/adr/100-compositions-are-validated-diagrams-are-projections.md`, decisions
D1–D5) and the design's primitives P1–P7 (`docs/proposals/gh1089-flow-boundary-design.md`) to the code, spec delta,
and test that carry them. Every `__` is replaced with the measured `file:line` at the head that carries the last
change to any `.go` file or spec delta on the branch (tasks 7.3). `tasks.md` rows cite section numbers.

| # | Decision / primitive | Implementation | Spec delta | Test / evidence |
|---|---|---|---|---|
| D1 | The unit of composition is `config.Components` + the catalog; connections are derived; a diagram is a projection | `composition/validate.go:__` (`Validate`), `composition/graph.go:__` | `specs/composition-validation/spec.md` "Composition validation is a pure function…", "…projection…" scenarios | `TestValidateIsDeterministic`, `TestGraphProjectionMatchesAdmittedComposition` |
| D2 | Port declarations are static facts of a factory; boot verifies them | `component/registry.go:__` (`Registration.Ports`, nil rejection), `:__` (parity compare in `prepareComponent`); 33 factory declarers (`tasks.md` 3.1 list) | "Port declarations are static facts of a registration" | `TestRegisterFactoryRejectsNilPortDeclarer`, `TestAdmissionRefusesPortDeclarationMismatch`, `TestDeclaredPortsMatchConstructedPortsForEveryRegisteredFactory` |
| D3 | One validator, one vocabulary, two evidence classes; boot refuses on error | `composition/findings.go:__` (13 constants + severity table), `composition/analyze.go:__`; `service/component_manager.go:__` (`Initialize` runs `Analyze`, refuses); `service/component_manager_http.go:__` (projection only) | "…one closed findings vocabulary", "Boot validates the admitted composition at the real boundary" | `TestValidateFindingsVocabularyIsClosed`, `TestComponentManagerRefusesBootOnErrorFinding`, `TestFlowValidationHandlerProjectsLibraryResult` |
| D4 | Products get `catalog`/`validate`/`graph`; no authoring store or write verb | `composition/cli/main.go:__`; `cmd/semstreams/main.go:__`; `composition/assert.go:__`; `processor/agentic-tools/executors/composition_tools.go:__` | "Every binary can expose the composition verbs…", "Product CI can assert…", "Agents read the catalog…" | `TestCLIValidateExitsNonZeroOnErrorFindings`, `TestCLICatalogPrintsEveryRegisteredFactory`, `TestCLIGraphMermaidRendersEveryEdge`, `TestValidateFlagReportsCompositionFindings`, `TestAssertValidFailsOnErrorFinding`, `TestValidateCompositionToolReturnsFindings`, `TestCompositionGraphToolReturnsMermaid`, `TestListComponentsCarriesPorts` |
| D5 | Retirement without aliases; the override-expiry metric survives | deletions per `tasks.md` 3.8 (grep count recorded there); `service/__:__` (rehomed reporter) | `specs/flow-authoring/spec.md` (11 REMOVED), `specs/component-runtime-config/spec.md` (1 REMOVED), "The framework owns no composition authoring store" | `TestServiceRegistryHasNoFlowBuilder`, `TestToolRegistryHasNoFlowTools`, `TestOpenAPIHasNoFlowRoutes`, `TestStreamOverrideExpiryReporterRegistersWithoutFlowService` |
| P1 | Catalog export carries default ports | `cmd/openapi-generator/main.go:__`; `composition/catalog.go:__` | "…the catalog carries default ports…" scenario | `TestCatalogCarriesDefaultPortsOrRequiresConfig`, `TestSchemaExportCarriesDefaultPorts` |
| P2 | The move from `engine` is complete (dropped-step detector) | `composition/engine_parity_integration_test.go` (exists until 3.8; its final green run recorded in `tasks.md` 3.2) | — | `TestValidateMatchesEngineFindingsForShippedConfigs` (deleted with the engine; last green output pasted in 3.2) |
| P5 | Shipped compositions validate clean before the refuse flips | `tasks.md` 3.5 measurements | "Shipped compositions carry no error finding" | `TestValidateShippedConfigsHaveNoErrorFindings` |
| P6 | Mermaid | `composition/mermaid.go:__`; `service/component_manager_http.go:__` (`format=mermaid`) | "…projection…" scenarios | `TestMermaidIsDeterministic`, `TestCLIGraphMermaidRendersEveryEdge` |
| E2E | BREAKING gate | `tasks.md` 6.6 | — | `task e2e:core`, `task e2e:crud-tools`, `task e2e:agentic` summaries |
| DEVIATION | (none recorded) | | | |
