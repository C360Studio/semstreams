# Conformance — single-type-authority (filled 2026-08-26 at branch head `1ec9775b`+; every `file:line` measured at that head)

Per-decision map from the owner's direction on #1100 (2026-08-26) and ADR-103 to the code, spec delta, and test that carry
each. Every `file:line` is to be measured at the head that holds the last change to any `.go` file or spec delta on the
branch; `tasks.md` rows cite section numbers. Fill the right-hand columns at implementation time; a row with an empty
Implementation column at review time is a deviation to record, not a gap to hide. Owner-item numbers follow the design §15.

| # | Ruling / decision | Implementation | Spec delta | Test / evidence |
|---|---|---|---|---|
| D1 | The payload registry is the single type authority (ADR-103 d1) | `payloadregistry/registry.go:1-20` (package contract), `:98-151` (`Register`: nil/factory/domain/category/version/schema/profile/contract checks, duplicate rejection); `payloadbuiltins/register.go:33-57` (the builtin set incl. `:53-54`) | `specs/payload-registry/spec.md` ADDED "A message type is a type of the deployment only if it is registered…" | `TestPayloadRegistryIsTheSingleTypeAuthority` PASS; `TestRegistry_RegisterPayload_DuplicateError` PASS; `TestRegisterRejectsSchemaMismatch` PASS |
| D2 | Contract and floor are attributes registered with the type, not parallel tables (d2) | `payloadregistry/registry.go:66,71` (fields), `:176-207` (`bindContracts`), `:240-283` (copies, `IndexingProfileFor`, `Contracts`); `pkg/projection/contract/contract.go:7-149` (leaf); `pkg/projection/contract.go:1-34` (aliases); `internal/builtinprojection/` deleted; `processor/graph-ingest/indexing_profile_registry.go` deleted | payload-registry ADDED "A registration carries…", "The registry exposes floor and contract lookups"; `projection-mutation-client` MODIFIED "Projection contracts are local schemas" | `TestRegisterFillsAndChecksContractMessageType`, `TestContractsReturnsIndependentSortedCopies`, `TestIndexingProfileFor`, `TestGetRegistrationCopiesAttributes` PASS; `grep -rn builtinprojection --include='*.go' .` → 1 (an assertion message naming the retired set); `grep -rn indexingProfileDefaults` → 0 |
| D3a | `EntityState.MessageType` is always a registered key — mutation lane rejects `message_type_unregistered` (d3) | `graph/mutation_responses.go:47`; `processor/graph-ingest/canonical_mutations.go:208-224` (helper), `:237` (RPC call site after `IsValid`); `mutation_runtime.go:44-58` meters and WARNs (reason = code, error text names the key) | `specs/graph-ingest/spec.md` ADDED "A birth MUST carry a registered message type" | `TestCreateRejectsUnregisteredMessageType`, `TestCreateAcceptsRegisteredMessageType` PASS (integration, `ok 27.815s`); `TestFactoryRejectsNilPayloadRegistry` PASS; omission (c) |
| D3b | Readers, codec, boot sweep, and the Graphable merge path never consult the registry (d3; L3) | no reader changed: `graph/entity_predicate_contract.go`, `component.go` sweep/`MergeEntity` untouched (`git diff 08660fc5 -- graph/entity_state*.go` empty) | `specs/graph-state-contract/spec.md` ADDED "The canonical codec and the boot sweep never consult the payload registry" | `TestResidentUnregisteredStampIsNotPoison` PASS (integration) |
| L1 | Premise for option A: the framework's writers call `internal/graphmutation` directly; only the ingest gate covers them | gate at ingest, not the client: `canonical_mutations.go:237`; writers unchanged in shape (`emit_lesson.go:514`, `emit_diagnosis.go:193`, `graph_writer.go:251`, `httprequest.go:260`, `websearch.go:258`) | graph-ingest ADDED (gate requirement) | `TestCreateRejectsUnregisteredMessageType` drives `graphmutation.Client.Create` over the wire |
| L2 | Nil registry at the seam is fail-closed; fixture helper + 23-literal sweep in the same change (O-15) | `canonical_mutations.go:209-216` (`rejectInternal` + ERROR); `processor/graph-ingest/component_fixture_test.go:93` (`withTestRegistry`) applied to the 23 literals; `service/message_logger_census_test.go:403` (census registry) | graph-ingest ADDED scenario "a create with no registry configured is refused" | `TestCreateSeamRejectsWhenRegistryMissing` PASS; omission (h); tasks 5.4 |
| DV1 | Floors are per-binary because registrations are (O-14; D1) | `agentic/research/register.go` floors exist only where `graphresearch.Selected` registers them (`cmd/semstreams/main.go:766-770` unchanged) | payload-registry ADDED (per-binary clause) | `TestIndexingProfileFor` (`research.result.v1` → unregistered on a registry without research) PASS |
| L5 | New import edge named in the package comment | `payloadregistry/registry.go:9-17` | — | `go list -deps ./payloadregistry` → `pkg/retry pkg/errs pkg/types pkg/platform vocabulary pkg/projection/contract payloadregistry` (tasks 3.2) |
| B-1 | In-process create path gated by the same helper; containers carry a registered type (O-16 (a)) | `component.go:2132` (helper on `createEntityWithReceipt`); `graph/inference/hierarchy.go:430` (stamp); `graph/inference/container_entity.go:18,62` | graph-ingest ADDED (in-process clause + container scenario); payload-registry ADDED (container row) | `TestInProcessCreateRejectsUnregisteredType` PASS; `TestHierarchyContainerBirthCarriesRegisteredType` PASS (integration); `task e2e:structural` GREEN (125 hierarchy provenance entities); omissions (j), (k) |
| F-1 | Contract relation: birth ⊆ Triples(full) ⊆ birth ∪ groups | `agentic/loop_execution_entity.go:204`, `agentic/agent_lesson_entity.go:178` (contracts beside the builders) | payload-registry ADDED (fourth requirement) | `TestRegisteredContractMatchesTriples` PASS; omission (e) |
| F-2 | Builders byte-identical; web observation `Tool` discriminator; diagnosis confidence | `agentic/web_observation_entity.go:37-63,96-125`; `agentic/ops_diagnosis_entity.go:75-108` (`Confidence: e.Confidence` `:87`, `%g` `:93`); `agentic/model_endpoint_entity.go:65-100` | — | `TestWebObservationEntityMatchesToolBuilders`, `TestOpsDiagnosisEntityMatchesBuilder`, `TestModelEndpointEntityMatchesBuilder`, `TestEmitLessonBuildsEntityTriples` PASS against goldens captured from the former builders at `08660fc5` (`tasks 2.3`); omission (m) |
| N-3 | Composition-root wiring `WireGraphRuntime(payloadReg.Contracts()...)` is mutation-checked | `cmd/semstreams/main.go:221`; `cmd/e2e-semstreams/main.go:155` | — | omission (i): `go build ./...` ok; `cmd/semstreams` boot → `Error: wire graph runtime: build graph mutation client: projection: invalid contract: no contracts`; `task e2e:core` app container exited(1) |
| F1 | Writer census: six `entityBucket` writers, four birth-capable, one decode-gated (`:1985`), two helper-gated | helper on the two births: `canonical_mutations.go:237` and `component.go:2132`; `MergeEntity` unchanged | graph-ingest ADDED (gate requirement) | `TestInProcessCreateRejectsUnregisteredType`, `TestCreateRejectsUnregisteredMessageType` PASS |
| F4 | Every moved builder has a golden test | `agentic/entity_payloads_test.go` (three goldens), `processor/agentic-tools/emit_lesson_entity_test.go` | payload-registry ADDED scenario "moved builders are byte-identical" | the four golden tests PASS; goldens captured at baseline with throwaway in-package dumps (`tasks 2.3`) |
| F7 | Hierarchy on + registry lacking the container type is a construction error (O-16 (a)) | `component.go:677-687` | graph-ingest ADDED scenario | `TestFactoryRejectsHierarchyWithoutContainerType` PASS; omission (l) |
| D3c | Floor read from the registered type; metric means "registered type with no floor" | `component.go:1901-1911` (`registeredIndexingProfile`), `:114-118` (help), `reconcileIndexingProfile` comment | graph-ingest ADDED "The indexing-profile floor is read from the registered type" | `TestFloorComesFromRegistration` PASS (incl. the lesson-floor subtest); rewritten `indexing_profile_registry_test.go` (22 expectations) PASS; omission (d) |
| D4 | Framework mutation-lane types are registered Graphable payloads with factories (d4) | `agentic/payload_registry.go:52-56`; `pkg/lifecycle/harness_entity.go:65`; `graph/inference/container_entity.go:62`; `payloadbuiltins/register.go:53-54` | payload-registry ADDED "Framework entity types born on the mutation lane…"; `agentic-lessons` MODIFIED; `lifecycle` ADDED | five `_RoundTrip` tests, `TestHarnessEntity_RoundTrip`, `TestRegisteredContractMatchesTriples`, `TestEmitLessonBuildsEntityTriples`, `TestWebObservationBirthIsRegistered` (integration, `ok 4.445s`) PASS |
| D4-snap | `LessonProjectionContract()` returns the registered contract | `processor/agentic-tools/lesson_promotion.go:53` → `agentic.LessonContract()` | `agentic-lessons` MODIFIED "External lesson composition uses the framework-owned contract snapshot" | `TestLessonProjectionContractIsTheRegisteredContract` PASS |
| D5 | Sister obligation; BREAKING; tag gate (d5, Consequences) | `docs/operations/migration-beta162-to-beta163.md:10-130` (per-sister sections; `:20` boot-time consequences added); no sister edits | — | tasks 7.3: `e2e:agentic` GREEN, `e2e:structural` GREEN, `e2e:core` and `e2e:lessons` FAIL on the production-binary premise (`[~]`, escalated); web-observation integration test PASS |
| C1 | `_Distinct` tests replaced by registry collision detection | four `_Distinct` functions deleted (`git grep _Distinct agentic/` → 0) | payload-registry ADDED (collision scenario) | `payloadbuiltins/register_test.go` PASS; `TestRegistry_RegisterPayload_DuplicateError` PASS |
| C2 | One-table test | `payloadbuiltins/single_type_authority_test.go` | — | `TestPayloadRegistryIsTheSingleTypeAuthority` PASS; omissions (a), (b) |
| C3 | e2e and unit fixtures register what they stamp | `cmd/e2e-semstreams/fixtures/register.go:24,68`; `cmd/e2e-semstreams/main.go:375`; `processor/graph-ingest/component_fixture_test.go:17-107`; out-of-package registries in `graph-index`, `rule`, `gated-dag` ×2, `agentic-tools`, `agentic-loop` tests | — | `TestFixturesRegisterEveryE2EStamp`, `TestBuildPayloadRegistryRegistersEveryE2EStamp` PASS; **`[~]`**: four tiers (core, lessons, crud-tools, research-graph) boot `target: production` = `cmd/semstreams`, which the fixtures cannot reach — escalated in tasks 6.1/7.3 |
| O-1 | ADR-103 accepted as worded | owner ruling on #1100, 2026-08-26 | — | — |
| O-2 | `pkg/projection/contract` leaf + aliases approved | `pkg/projection/contract/contract.go`; `pkg/projection/contract.go` | projection-mutation-client MODIFIED | `pkg/projection/contract_test.go` GREEN unchanged; `TestContractLiteralCompilesAgainstAliases` PASS. Exports added beyond O-2's list: `contract.ValidateShape` (listed), `agentic.{RuleFields on the five types}` (forced by #1052's registry census), `fixtures.Carrier` (cmd-local) — see the PR return |
| O-3 | Floors | `agentic/payload_registry.go:52-56` (control, content, content, control, content); `pkg/lifecycle/harness_entity.go:72`; `graph/inference/container_entity.go:69` | payload-registry ADDED (floor list) | `TestPayloadRegistryIsTheSingleTypeAuthority` PASS |
| O-4 | No new birth contracts (defer to #818) | only loop execution and lesson carry `Contracts` (`agentic/payload_registry.go:52-53`) | payload-registry ADDED (contract clause) | `TestRegisteredContractMatchesTriples` runs on the two |
| O-5 | Empty floor metered (not rejected) | `payloadregistry/registry.go:136-143` (only an invalid non-empty profile is refused); `component.go:1901-1911` | payload-registry ADDED scenario "a registered type may declare no floor" | `TestIndexingProfileFor`, `TestFloorComesFromRegistration` PASS |
| O-6 | OVERRIDE — eight-tier tag gate + `TestWebObservationBirthIsRegistered` | — | — | run in this PR: `e2e:agentic` GREEN (21:08:26Z–21:09:31Z), `e2e:structural` GREEN (21:10:22Z–21:10:42Z), `e2e:core` FAIL, `e2e:lessons` FAIL (premise, `[~]`); `TestWebObservationBirthIsRegistered` PASS; the full union is the TAG gate, not this PR's |
| O-7 | Wave order | the moved contracts carry the current patterns verbatim (`agentic/loop_execution_entity.go:208`, `agentic/agent_lesson_entity.go:182`) | — | — |
| O-8 | Checklist gains floor + contracts + `RegisterTestType` | `.agents/skills/new-payload/SKILL.md:78-127,203-206`; `docs/concepts/15-payload-registry.md:235-284` | — | Step 3 blocks byte-identical apart from heading level (tasks 6.3) |
| O-9 | ops seed key corrected | `test/e2e/scenarios/ops/scenario.go:462-464` | — | `task e2e:ops` not run in this PR (e2e target; see tasks 7.3) |
| O-10 | web_observation e2e coverage gap | not filed (sisters read-only does not apply; no issue filed by this implementer) | — | `TestWebObservationBirthIsRegistered` PASS — FILE: line in the PR return |
| O-11 / O-12 | Sisters read-only; obligations in the migration doc | `docs/operations/migration-beta162-to-beta163.md` (unchanged pins; `:20` added) | — | no sister file written or command run |
| O-13 | `Contract.IndexingProfile` retained with agreement check | `payloadregistry/registry.go:196-204` | payload-registry ADDED (shape validation clause) | `TestRegisterRejectsInvalidIndexingProfile` (subtest "contract profile must agree with the type floor") PASS |
| O-14 | Per-binary floors | as DV1 | payload-registry ADDED | `TestIndexingProfileFor` PASS |
| O-15 | Fail-closed seam on nil registry | as L2 | graph-ingest ADDED | `TestCreateSeamRejectsWhenRegistryMissing` PASS; omission (h) |
| O-16 | Hierarchy containers registered with the factory check | as B-1, F7 | graph-ingest ADDED; ADR-103 d3 | `TestHierarchyContainerBirthCarriesRegisteredType`, `TestFactoryRejectsHierarchyWithoutContainerType` PASS; omissions (k), (l) |
| O-17 | `MutationClient.Create` fills an empty stamp; conflicting stamp rejected | `pkg/projection/mutation_client.go:147-154` (fill), `:334-336` (conflict names both keys), `:343` (key splitter) | `projection-mutation-client` ADDED "Create fills an empty entity message type from the bound contract" | `TestCreateFillsMessageTypeFromContract`, `TestCreateRejectsConflictingMessageType` PASS; omission (n) |
| O-18 | Two distinct registration acts | no entity-ID-domain registration touched | — | — |
| DEVIATION | **Tier-to-binary premise (tasks 6.1, §8 L4, §11 tier map).** The design assumed `cmd/e2e-semstreams` hosts graph-ingest for every tier; measured: core, lessons, agentic, crud-tools, research-graph, deep-research boot `target: production` (`cmd/semstreams`), only structural/statistical/semantic/lifecycle/ops boot `target: e2e`. The fixtures package cannot reach the production binary, so the tiers that stamp `test.fixture.v1` (core, lessons), `e2e.probe.v1` (crud-tools) and `research.e2e_search_seed.v1` (research-graph) are refused. Escalated, not executed — owner sign-off pending (options in tasks 6.1) | | | `task e2e:core` → `core-graph-roundtrip` FAIL: `entity message_type "test.fixture.v1" is not registered…`; `task e2e:lessons` → `create evidence fixture: … "test.fixture.v1" is not registered…` |
