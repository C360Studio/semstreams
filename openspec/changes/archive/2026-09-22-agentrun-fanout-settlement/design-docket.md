## Owner docket — #1249 layer design (AgentRun milestone fanout onto the typed delivery API; `ConsumeWithHeartbeat` removed), 2026-09-18

Design draft 4 passed an independent inventory pass (162 pins, verifier exit 0 at base `0053183d`, six moved citations attributed to L1 head `c2a9cef6`) and two design-review passes; the coordinator verified the final narrow round. Standing owner rulings applied: greenfield, no deprecation; the helper is deleted by this layer with a migration note; sisters read-only (semdev breaks at two direct call sites plus three comment-only sites that size `max_deliver` on the removed 30s NAK; `DelayedDeliveryRetry(30s)` preserves that budget).

Coordinator rulings already recorded in the design (not owner questions): `ErrWorkflowNotRegistered` → Quarantine (process-wide constant; Terminate would silently drop every milestone); `ErrEntityNotLifecycleManaged` → bounded Retry (ADR-049 Q5 forward reference, resolved by a later `Create`); deterministic AgentRun-side errors classified `errs.WrapInvalid` at origin → Terminate; `pkg/lifecycle` untouched.

Five questions, each with the recommendation first:

| # | Question | Recommendation | Why |
|---|----------|----------------|-----|
| Q1 | An attempt where every handler reports invalid input: Terminate or Quarantine? | **Terminate** with WARN + counter reason | Invalid is per-message, never per-process; Quarantine would latch the lane for one bad payload. Matches the two decode-failure rows. |
| Q2 | Milestone consumer redelivery bound: `MaxDeliver N` with the framework's existing `semstreams_nats_max_delivery_exhaustions_total` advisory as the stuck-milestone signal, or `0` with no signal? | **N = 5**, `DelayedDeliveryRetry(30s)`, ≈2 min budget | Reverses the coordinator's earlier "0 + attempt histogram" recommendation: the histogram cannot be built (L0 withdrew the attempt count at `266453eb`) and `0` never fires the advisory `internal/maxdelivery/observer.go` already registers. 5 over the port default 3 because each attempt re-runs every handler against one 30s deadline. Invariant I6 guards against drift back to 0. |
| Q3 | Exported additions on Tier 1 `agentic/agentrun`: `LoopTerminalEvent.SourceMessageID`, `MilestoneSubscriber.DeliveryFatal() error`, `MilestoneSubscriber.RegisterMetrics(metric.MetricsRegistrar) error`, one metric `semstreams_agentrun_milestone_decisions_total{lane,decision,reason}` | **Yes to all four** | `SourceMessageID` is the idempotency key handlers replay on (same key dispatch uses); `DeliveryFatal()` is how the latch reaches `/health` across the `milestoneStarter` seam; `RegisterMetrics` is the only live registration path (`Service.RegisterMetrics` is called by nothing, `service/storage_observability.go:246`); without the counter the only visibility is the exhaustion advisory. |
| Q5 | An env-gated, test-only `MilestoneHandler` in `cmd/e2e-semstreams` so the #1155 stage-D proof can observe handler re-invocation (both boot roots are hand-copied, #1301) | **Admissible** | No production composition registers a handler today (measured in-tree and across every sister), so nothing else can observe the fanout end to end. |
| Q6 | Amend #1155's "matching milestone transition" assertion to handler re-invocation + idempotent effect count, per ADR-053 D5 (observe, never predict) | **Yes** | Under whole-fanout replay the observable is "handler ran N times, effect applied once", not a transition. |

Also declared, no question: the helper's removal is a Tier 1 incompatible removal landing as a `!` commit; `scripts/api-compat.sh` has no waiver mechanism (report mode exits 0 with the count, strict exits 1), ADR-106 pre-RC permits a descending count; the agentic E2E tier must be green at the merge head.

Reply with a per-row ruling ("as recommended on all five" suffices). The design and inventory are in the coordinator's scratchpad and are copied into `openspec/changes/agentrun-fanout-settlement/` when the layer's worktree opens (after L1 lands).
