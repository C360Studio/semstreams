# Design review, pass 3 — application check + two directed walks

semstreams-reviewer, read-only, from `/Users/coby/Code/c360/semstreams-wt/verify-68c14c8e` (HEAD = 68c14c8e…, porcelain 0). Scope: verify the coordinator's applied rulings; pass-2-closed items not re-reviewed.
```
scripts/inventory-verify.sh <abs>/l4/change/inventory.md          → pins=162 ok=162 moved=0 ambiguous=0 drift=0 malformed=0 unparsed=0, changed since base: (none), EXIT=0
sed -n '1096,1110p;1135,1165p' processor/agentic-loop/state.go ; sed -n '2608,2616p;2459,2482p' processor/agentic-loop/handlers.go
grep -rn 'PendingApproval\.RequestID = ' processor/agentic-loop/*.go agentic/*.go ; sed -n '174,198p' agentic/state.go   # BeginAwaitingApproval
sed -n '/Cold read, step 0/,/^## 4\./p' design.md ; sed -n '/### 5.5 /,/### 5.6/p' design.md ; grep -n '^- I2\.\|^- I4\.' -A3 design.md
sed -n '/2\.5 `settlement/,/^## 3/p;/4\.1 New/,/^## 5/p' tasks.md ; sed -n '/cold replacement adopts/,/A stale/p' specs/agentic-loop/spec.md
```
**Applied as ruled, verified in the files:** § 3.6 writes the four fields plus the gate clear and `State = running`, and names `validatedToolBatchResults` (`ST:441-443`) as never seeing a synthetic entry. I2 carries "An adopted record carries an empty set; I2 holds vacuously until the next batch"; I4 carries the same-update clearing sentence. § 5.5 step 1 runs step 0 first and its W4 paragraph reproduces the mechanism with the right pins (`ARH:130-149`, `:269` before `:275`, `AS:156`, `H:2654`) and resolves through W3. § 7 items 13/14 renumbered, item 3 extended. tasks 2.5 (fields + the `awaiting_approval` adopt test case), 4.1 (approval-response lane in the generator, reject-minted W4 named), 4.2 (the restart case with its three assertions), 4.3(f) (drop the gate clear → fails on I4) all present. Spec delta: the adopt scenario's THEN now reads "`pending_tool_results` empty, any `pending_approval` cleared with `state = running`", and the new rejection-minted scenario sits under the same ADDED requirement. HIGH and MEDIUM from pass 2 are genuinely dissolved rather than answered — nothing is synthesized, so there is no message-derived source and no Name-less entry.

**(1) Cold W4 on the approval lane, end to end.** Crash between `ARH:269` and `:275` leaves R(N+1) retained, record `awaiting_approval` at R with R's batch stored. Redelivery is necessarily cold (the crash killed the process, so `ARH:176`'s warm branch cannot hit). Step 0 at `ARH:182`'s revision read adopts: one CAS `Update` sets `PublishedRequestID=R(N+1)`, `Iterations=N+1`, `PendingToolResults=nil`, `PendingApproval=nil`, `State=running`. Step 1 then hits `ARH:200` — `persisted.State != LoopStateAwaitingApproval` — and returns `DeliveryDecisionAck` at `ARH:206` with the existing metric at `ARH:204`. That return precedes `ARH:217`, so `recoverApprovalResponse`, `restoreToolBatch` and `validatedToolBatchResults` are **unreachable** in this window, and so is § 5.5 step 2's I4 check. Nothing validates the adopted record's empty set.
**No surviving reader depends on R(N)'s entries after the advance.** Enumerated: § 5.6's verdict path checks `verdict.RequestID` older *before* the `ExecutionID ∈ PendingToolResults` branch, so an R(N) verdict still ACKs (different branch, same disposition); Q7's terminal-unproven check and the spec scenario read *absence*, which an empty set satisfies; § 5.4's gate-consumed branch is behind § 5.3 step 2's older-classification, which fires first; `validatedToolBatchResults`' two loops no-op on an empty map and `ST:486`'s advance guard is false anyway (and its `Iterations--` is deleted); `validatePendingApprovalResult` (`SR:1015-1032`) is gate-only and the gate is gone. Every reader is either gated behind a RequestID classification or unreachable.

`MEDIUM design.md § 3.6 (and inventory pin state.go:1161) — the stated rationale for the empty set is false at the pinned code; the mechanism survives it`
- § 3.6 calls `PendingToolResults = nil` "the shape the normal path leaves at the advance (`GetAndClearToolResults`, `ST:1139`/`:1161`, clears the batch before R(N+1) is minted)". The advance does not call it: `handleToolsComplete` calls `h.loopManager.toolResults(loopID, false)` at `H:2612`, and `toolResults` nils the map only when `clearResults` is true (`ST:1161`). The doc comment at `ST:1144-1146` states the opposite of the design: "The next-model transition **retains** its durable evidence until the new request's first result replaces the batch; explicit drains still clear it immediately." Eviction happens one step later, at `StoreToolResult` (`ST:1100-1105`), when R(N+1)'s first result lands.
- So the adopt writes a record one eviction *ahead* of what the normal path writes — which the walk above shows is harmless, but the design asserts equivalence rather than the real reason.
- Fix, one sentence: nil is the shape the normal path reaches at `ST:1100-1105` rather than at the advance, and it is safe because every reader of the stale batch is gated behind an older-RequestID classification (the enumeration above). Leave `GetAndClearToolResults` out of it.
- Verification: `sed -n '2608,2616p' handlers.go`; `sed -n '1144,1165p' state.go`. Refutation attempted and failed — no other advance-path caller drains the map.

**(2) Can the adopt clear a gate that belongs to the adopted request? No — and there is a stronger pin than I4.** `gateForApproval` stamps `entity.PendingApproval.RequestID = toolResult.RequestID` at `H:2468` (`BeginAwaitingApproval`, `agentic/state.go:174-197`, sets no RequestID), and a tool result's RequestID is the request whose response proposed the call, i.e. the then-current `PublishedRequestID`. So at write time gate.RequestID == PublishedRequestID as a fact of the code, not only as I4 — and the adopt fires only when the retained request is strictly newer, hence strictly newer than the gate's. Independently: a gated batch cannot advance at all, because any stored `IsApprovalRequired` result sets `ordinaryBatch = false` (`ST:461-464`) and `ST:486`'s advance guard requires it, while the only other minting site, the truncation retry (`H:1981`), fires on a model response a gated loop has none outstanding. Two independent reasons; I could not construct a counterexample.

`DESIGN REVIEW PASS` — 0 BLOCKING, 0 HIGH, 1 MEDIUM (a false rationale, not a false mechanism), 0 NIT. Both pass-2 blockers are closed by a mechanism I verified rather than by an assurance; the directed walks confirm the coordinator's two arguments, one of them on firmer ground than claimed. Fix the § 3.6 sentence before implementation so the next reader does not inherit it. Not owner approval; runtime implementation and spec promotion stay blocked until the owner accepts the reviewed design.
