# Beta.161 user-response current truth

## Why

The immutable #952 package mixes accepted SemStreams framework requirements with product-owned SemDev and SemTeams
requirements. Its ten checkpoint-covered artifacts cannot be edited, while current SemStreams capability truth must
contain only framework-owned behavior.

## What Changes

- Promote the four framework-owned typed-response, rule-guard, governance-removal, and declaration-truth requirements.
- State the fresh-storage and downstream-ownership boundary without product implementation gates.
- Preserve the immutable #952 package unchanged as historical evidence.

## Impact

This is specification truth only. It changes no runtime, generated schema, sister repository, or external issue.
