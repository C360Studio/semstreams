# Conformance — flow-authoring-retirement

Per-decision map from ADR-100 decision D5 ("Retirement without aliases") and the ADR's superseded clauses to the code,
spec delta, and test that carry them. Every `__` is replaced with the measured `file:line` at the head that carries the
last change to any `.go` file or spec delta on the branch (tasks 7.3). `tasks.md` rows cite section numbers. Decisions
D1–D4 and primitives P1–P7 were carried by `composition-validation-substrate` (PR #1101, #1092); its conformance table
is the record for those and is not restated here.

| # | Decision / clause | Implementation | Spec delta | Test / evidence |
|---|---|---|---|---|
| D5.a | The framework registers no `flow-builder` service | `service/register.go:9-19` — the `services` map; the `"flow-builder": NewFlowServiceFromConfig` row and `service/flow_service.go` are removed | `specs/composition-validation/spec.md` "The framework owns no composition authoring store" | `TestServiceRegistryHasNoFlowBuilder` (`service/register_test.go:29`); forced omission `tasks.md` 4.2 |
| D5.b | The framework registers none of the eleven flow / flow-template tools | `processor/agentic-tools/executors/register.go:45-56` (`ToolDependencies` — the two manager fields removed), `:113-118` (`BuiltinGroupKeys` multi-tool block — `flows` and `flow_templates` removed), `:200-202` (the gate list — the two gates removed); `flows.go`, `flow_templates.go`, `register_flows.go`, `register_flow_templates.go` removed | same requirement | `TestToolRegistryHasNoFlowTools` (`processor/agentic-tools/executors/register_test.go:501`); forced omission `tasks.md` 4.3, which exposed and closed a dispatch-key hole in the guard |
| D5.c | The generated OpenAPI document serves no `/flowbuilder` or `/flows` path and carries no `Flow*` schema | `specs/openapi.v3.yaml` −721 lines: 36 paths remain, none under `/flows`; the four `Flow*` and four runtime/publish response schemas are gone. The `FlowGraph` tag at `:1759` is the RETAINED projection surface (`/flowgraph` `:105`, `/paths` `:476`, `/validate` `:595`). No component factory was touched, so no `schemas/*.v1.json` CONTENT changed; one schema FILE is deleted under the #1122 ruling — see row D5.h | same requirement | `TestOpenAPIHasNoFlowRoutes` (`test/contract/openapi_no_flow_routes_test.go:41`); forced omission `tasks.md` 4.4 |
| D5.h | `schemas/workflow-definition.v1.json` and its contract-test exemption leave with the retirement (**owner ruling on #1122, 2026-08-27**; added after the proposal was first written) | `schemas/workflow-definition.v1.json` deleted (233 lines); `test/contract/schema_contract_test.go` — the `nonComponentSchemas` map (was `:17-21`) and all four skip sites (was `schema_contract_test.go:64,113,237` and `schema_export_test.go:30`) removed, the map DELETED rather than emptied so no file is exempt; `cmd/openapi-generator/main.go` — the stale "Workflow definition schema generation removed" note deleted with the artifact it described. `schemas/` = **33 files = 33 registered factories**; `grep -c nonComponentSchemas test/contract/*.go` → 0 in every file | `specs/composition-validation/spec.md` "The framework owns no composition authoring store" (the framework publishes no artifact for a surface it does not own) | Four contract guards, proven load-bearing by restoring the deleted artifact (`git show origin/main:schemas/workflow-definition.v1.json`, sha256 `7938cc0671725d535a1c64e2f61963d2b0e991d91c394eedaa69bd5f49b4c61c`) and re-running `go test ./test/contract/...` — all four went RED: `TestCommittedSchemasMatchCode` (`test/contract/schema_contract_test.go:19`), `TestCommittedSchemasValidStructure` (`:86`), `TestNoOrphanedSchemaFiles` (`:192`, `Orphaned schema file: workflow-definition.v1.json (no corresponding component registered)`), `TestSchemaExportCarriesDefaultPorts` (`test/contract/schema_export_test.go:15`). Artifact re-deleted → green. `task schema:generate` twice → the generator does NOT re-create it. Transcript in `tasks.md` 5.1 |
| D5.h-migration | The published-artifact break reaches the two repositories that vendor it | — | — | `docs/operations/migration-beta162-to-beta163.md` § "`schemas/workflow-definition.v1.json` is deleted": semstreams-ui @ `39f5f04` deletes `contracts/semstreams/schemas/workflow-definition.v1.json` (vendored artifact only, no hand-written reference); semteams @ `8a70b7e7` deletes `schemas/workflow-definition.v1.json` **and** the exemption that hides it — `test/contract/schema_contract_test.go:14-18` with skip sites `:57`, `:106`, `:188` — because with the entry gone its own orphan guard fails on the vendored file |
| D5.d | The framework creates no `semstreams_flows` or `FLOW_TEMPLATES` bucket | `flowstore/` and `flowtemplate/` removed | same requirement | `tasks.md` 3.2 grep count |
| D5.e | The override-expiry metric survives the removal | `service/component_manager.go:74-85` (doc comment `:74-84`, field `:85`), `:237-260` (constructed from the LIVE config, registered against `deps.MetricsRegistry`, evaluated once at composition), `:534-556` (`supervise` runs the loop under a WaitGroup joined at `:556` before `supervisorDone` closes, so `Stop` joins it through `waitSupervisor`) | same requirement, second scenario | `TestStreamOverrideExpiryReporterRegistersWithoutFlowService` (`service/stream_override_expiry_test.go:220`) and `TestComponentManagerStartRunsOverrideExpiryReporter` (`:271`); forced omission `tasks.md` 4.1. **Join guards** (review HIGH-1, added because the three FlowService lifecycle guards for this property were deleted with the service and dropping `loops.Wait()` left `-race ./service/` green): `TestSuperviseHoldsDoneUntilTheOverrideExpiryLoopReturns`, `TestComponentManagerStopWaitsForTheOverrideExpiryLoop`, `TestComponentManagerFailedStartDoesNotLaunchOverrideExpiryLoop` — mutation transcript in `tasks.md` 7.1. **Reachability changed even though the contract did not**: the metric was previously registered only through `Service.RegisterMetrics`, which nothing in the framework calls (→ #1123), so the gauge reached `/metrics` in no process and the WARN fired only where `flow-builder` was enabled; adopter consequence stated in `docs/operations/migration-beta162-to-beta163.md` § "The stream-override expiry metric moved hosts" |
| D5.f | `flow-authoring` is retired as a capability | eleven requirements REMOVED | `specs/flow-authoring/spec.md` | the named tests are deleted with their packages (`tasks.md` 7.4 table) |
| D5.g | Flow publication is removed from `component-runtime-config` | `service/flow_service.go` removed | `specs/component-runtime-config/spec.md` (1 REMOVED) | `config.Manager.PutComponentToKV` has no route or tool in front of it (`tasks.md` 3.2 grep) |
| ADR-100 §Consequences | BREAKING e2e gate | `tasks.md` 6.6 | — | `task e2e:core`, `task e2e:crud-tools`, `task e2e:agentic` |
| DEVIATION | **Migration-document location.** The proposal originally required a separate `docs/operations/migration-composition-validation-adr100.md` (`tasks.md` 3.4). The material was written instead as a new `##` section, "Flow-authoring retirement (ADR-100 D5)", inside `docs/operations/migration-beta162-to-beta163.md`. Reasoning: that document's own header states the convention "One `##` section per landing; later wave items … append their own sections below"; #1101 set the precedent by placing the ADR-100 `/gaps` section there; and this section cross-references that one directly instead of across files, which a second document could not do. No content was dropped — removed routes, tools, packages and symbols, buckets, the deleted schema artifact, the `/paths` and metric changes, and per-repo instructions measured at each sister's pinned SHA are all present | `docs/operations/migration-beta162-to-beta163.md` § "Flow-authoring retirement (ADR-100 D5)"; `proposal.md` migration bullet reconciled to name this file; `tasks.md` 3.4 | — | **OWNER SIGN-OFF: ACCEPT, 2026-08-27** — "ACCEPT the location — the ADR-100 D5 material stays as a `##` section in `docs/operations/migration-beta162-to-beta163.md`; do not create `migration-composition-validation-adr100.md`." Raised by the Fable review, seconded by the Codex round ("I see no technical loss in the release-document location"), ruled by the owner |
| CARRIED | The duplicate graph build retained by PR #1101 (`GetFlowGraph` / `BuildFromRegistry` / `GET <components>/paths`) is re-judged here | RESOLVED, all three removed: `service/component_manager.go:1392-1466` (`GetFlowPaths` `:1405` walks `composition.Result.Graph`; `isInputNode` `:1433` and `reachableFrom` `:1449` replace the Registry rebuild, its cache and its DFS), `component/flowgraph/flowgraph.go` (`BuildFromRegistry` deleted outright; `BuildFromDeclarations` `:130` is the one remaining construction seam), `component/flowgraph/flowgraph_analysis.go:7-12` (`ValidationStatus` gone) — see `tasks.md` 3.3 | `specs/composition-validation/spec.md` MODIFIED: the `[~]` is replaced by SHALL clauses for all three, plus two new scenarios | `TestComponentManagerFlowReportingUsesRetainedPortsAfterComponentMutation`, `TestComponentManagerProjectionCarriesOnlyAdmittedInstances`, `TestComponentManagerPortReportingUsesCanonicalFactsWithoutDroppingKinds`, and `TestFlowPathsTraverseTheRetainedGraph` — the traversal guard added after self-review, covering all three origin forms (declared input type, network-listening port, outbound HTTP-client port); each arm mutation-verified (`tasks.md` 3.3, 7.1 MED-4) |
