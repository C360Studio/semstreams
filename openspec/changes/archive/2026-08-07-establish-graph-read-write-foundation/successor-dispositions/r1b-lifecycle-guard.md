# R1b successor disposition — lifecycle graph-state guard

GS-01 truthfully preserved the lifecycle Manager's graph-state guard at its coordinated landing. The later reviewed
R1b poison-localization decision, recorded by ADR-092, supersedes that mechanism as current framework behavior.

The active GS-01 target is amended only at its current-truth surfaces:

- the `framework-composition` delta no longer requires either composition root to wire or retain the lifecycle
  graph-state guard;
- checked task 7.3 retains completion history but marks its guard-preservation clause superseded;
- the catalog cleanliness check, generic graph-bucket write protection, mutation protocol, exact-read adapter, local
  projection dependencies, and sister-binary parity remain current.

The accepted GS-01 design, approval, reviews, inventory, and implementation evidence remain historical evidence and
are not rewritten by this disposition.
