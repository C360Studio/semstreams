# Inventory — delivery-lane-admission-package (#1341)

base: 3faca84f7891b57736bf02c863ac432f9525c899

Worktree `claude/gh1341-deliverylane`. Every pin below is generated by `sed -n "${n}p"` at this base and verified
with `scripts/inventory-verify.sh`. Pins into files outside this repository (the #1249 draft, the L4 draft) are quoted
in prose, not as bullets, because the script cannot verify them. The design round measured at `20fe8d09` (the pushed
L1 head); this revision is the re-pin `design.md` § 9 requires, at the merged L3 commit.

**Re-pin note (round 3, measured at the implementation base; `design.md` § 9).** The base is now `3faca84f`, the
squash-merge of PR #1338 (L3) on `main`; `20fe8d09` and `c58c65bd` are not ancestors of it (both branches squashed),
so every pin was re-derived POSITIONALLY — old blob vs new blob, `difflib` opcodes, then `sed -n "${n}p"` at the new
line — never by text search, which is ambiguous for the repeated lines this inventory pins (15 pins were AMBIGUOUS by
text at the new base, including a bare `}`). Result: 178 pins unmoved, 75 moved, 7 remapped into
`openspec/changes/archive/2026-09-19-settle-after-durable-effect/` (L1 archived), 0 with changed text, 9 gone.

Every premise the design rests on holds at `3faca84f`, measured not assumed: all five `delivery_owner.go` files are
tracked (`git ls-tree -r --name-only HEAD | grep delivery_owner.go` → five) and byte-identical to the design base
(`git diff --stat 20fe8d09 3faca84f -- 'processor/*/delivery_owner.go'` → empty; likewise `fbd3c173 3faca84f`, so L3
did not touch them), `wc -l` is still 136+122+116+101+75 = 550, static lane constructions are 8 (dispatch 3,
governance 1, loop 2, model 1, tools 1) and refusal declarers 3 — exactly the numbers the design predicted for the
L3 head.

Two measurements moved and are recorded rather than assumed. (1) The nine gone pins: eight are dispatch's
`agent.created` and `agent.approval_pending` lane sites, deleted by `2693df0e` — the re-MEASURE `design.md` § 9
named, so their bullets are dropped rather than re-pinned (`git grep -n 'agentCreated\|approvalPending' HEAD --
processor/agentic-dispatch/component.go` → zero hits); the ninth is one loop test line whose enclosing test,
`TestResponseAndToolResultPersistenceFailureCannotAck`, still exists and still reaches the surface, so the census
re-pins it at its new first reference. (2) The test census grew: 36 tests / 16 files / 4 integration-tagged at
`20fe8d09` → **45 / 23 / 5** here (§ 4). `tasks.md` 2.6 and its per-component gate line are re-scoped accordingly;
the extra integration-tagged file is `processor/agentic-loop/terminal_failure_record_integration_test.go`, which the
gate line did not name.

Review status: `INVENTORY PASS` recorded 2026-09-19 (round 1: 0 BLOCKING / 2 MAJOR / 3 MINOR, all closed in round
2 — MAJOR-1 drain-once census § 2 and § 5, MAJOR-2 mechanical test census § 4, MINOR-1 `openspec/project.md` pinned,
MINOR-2 governance wording § 1, MINOR-3 the base note) and `INVENTORY PASS` again in round 2 (0 BLOCKING / 0 MAJOR /
1 MINOR; MINOR-A, the dropped `terminal_settlement_integration_test.go` census row, is closed here — the census is
regenerated from the tree, not edited). Round 3 is this re-pin; it re-derives, it does not re-sweep.

## Problem statement

The per-lane delivery admission latch — a mutex-guarded `open` bool, a one-slot `fatal` channel, `admit`/`latch`,
a drain-once binding around the exact `jetstream.ConsumeContext`, and an observer goroutine that reacts to the first
`OwnerStopRequired` result by recording the fatal and draining that handle — is spelled once per agentic component.
The #1249 design copies it a sixth time into `agentic/agentrun`. Owner ruling 2026-09-19 (#1341): "agree - if we have
a legit abstraction lets add it after L3 so L4 does not need to do it all over." This inventory measures what exists
before any target state is drafted.

## 1. The claimed gap

The change claims no shared home for the latch exists and that five private spellings do. Both halves measured.
The five `type deliveryLaneAdmission struct` declarations, their constructors, the admission entry points, the
binding constructors, the drain-once methods, the observers, the panic wrappers, and the per-component fatal recorders:

- `processor/agentic-dispatch/delivery_owner.go:27` — `type deliveryLaneAdmission struct {`
- `processor/agentic-tools/delivery_owner.go:14` — `type deliveryLaneAdmission struct {`
- `processor/agentic-loop/delivery_owner.go:29` — `type deliveryLaneAdmission struct {`
- `processor/agentic-model/delivery_owner.go:14` — `type deliveryLaneAdmission struct {`
- `processor/agentic-governance/delivery_owner.go:20` — `type deliveryLaneAdmission struct {`
- `processor/agentic-dispatch/delivery_owner.go:35` — `func newDeliveryLaneAdmission(`
- `processor/agentic-tools/delivery_owner.go:22` — `func newDeliveryLaneAdmission(`
- `processor/agentic-loop/delivery_owner.go:36` — `func newDeliveryLaneAdmission(onFatal func(natsclient.DeliveryResult)) *deliveryLaneAdmission {`
- `processor/agentic-model/delivery_owner.go:21` — `func newDeliveryLaneAdmission(onFatal func(natsclient.DeliveryResult)) *deliveryLaneAdmission {`
- `processor/agentic-governance/delivery_owner.go:27` — `func newDeliveryLaneAdmission(onFatal func(natsclient.DeliveryResult)) *deliveryLaneAdmission {`
- `processor/agentic-dispatch/delivery_owner.go:57` — `func (a *deliveryLaneAdmission) admit() bool {`
- `processor/agentic-dispatch/delivery_owner.go:63` — `func (a *deliveryLaneAdmission) latch(result natsclient.DeliveryResult) {`
- `processor/agentic-dispatch/delivery_owner.go:51` — `func (a *deliveryLaneAdmission) refuse(subject string) {`
- `processor/agentic-tools/delivery_owner.go:38` — `func (a *deliveryLaneAdmission) refuse(subject string) {`
- `processor/agentic-dispatch/delivery_owner.go:80` — `func consumeAdmittedDelivery(`
- `processor/agentic-tools/delivery_owner.go:67` — `func consumeAdmittedDelivery(`
- `processor/agentic-loop/delivery_owner.go:74` — `func consumeAdmittedDelivery(`
- `processor/agentic-model/delivery_owner.go:60` — `func consumeAdmittedDelivery(`
- `processor/agentic-dispatch/delivery_owner.go:99` — `func newStreamConsumerBinding(handle jetstream.ConsumeContext) streamConsumerBinding {`
- `processor/agentic-tools/delivery_owner.go:86` — `func newStreamConsumerBinding(handle jetstream.ConsumeContext) streamConsumerBinding {`
- `processor/agentic-loop/delivery_owner.go:88` — `func newStreamConsumerBinding(handle jetstream.ConsumeContext) streamConsumerBinding {`
- `processor/agentic-model/delivery_owner.go:74` — `func newStreamConsumerBinding(handle jetstream.ConsumeContext) streamConsumerBinding {`
- `processor/agentic-governance/delivery_owner.go:54` — `func newStreamConsumerBinding(handle jetstream.ConsumeContext) streamConsumerBinding {`
- `processor/agentic-dispatch/delivery_owner.go:112` — `func (b *streamConsumerBinding) drain() {`
- `processor/agentic-tools/delivery_owner.go:99` — `func (b *streamConsumerBinding) drain() {`
- `processor/agentic-loop/delivery_owner.go:92` — `func (b *streamConsumerBinding) drain() {`
- `processor/agentic-model/delivery_owner.go:78` — `func (b *streamConsumerBinding) drain() {`
- `processor/agentic-governance/component.go:728` — `func (b *streamConsumerBinding) drain() {`
- `processor/agentic-dispatch/delivery_owner.go:119` — `func (c *Component) observeDeliveryLane(`
- `processor/agentic-tools/delivery_owner.go:106` — `func (c *Component) observeDeliveryLane(`
- `processor/agentic-loop/delivery_owner.go:99` — `func (c *Component) observeDeliveryLane(`
- `processor/agentic-model/delivery_owner.go:85` — `func (c *Component) observeDeliveryLane(`
- `processor/agentic-governance/delivery_owner.go:58` — `func (c *Component) observeDeliveryLane(`
- `processor/agentic-dispatch/delivery_owner.go:13` — `func runDispatchDeliveryWork(`
- `processor/agentic-loop/delivery_owner.go:12` — `func runLoopDeliveryWork(`
- `processor/agentic-governance/component.go:349` — `func runGovernanceDeliveryWork(`
- `processor/agentic-dispatch/component.go:732` — `func (c *Component) recordDeliveryOwnerFatal(result natsclient.DeliveryResult) {`
- `processor/agentic-tools/component.go:521` — `func (c *Component) recordDeliveryOwnerFatal(result natsclient.DeliveryResult) {`
- `processor/agentic-loop/delivery_owner.go:65` — `func (c *Component) recordDeliveryOwnerFatal(result natsclient.DeliveryResult) {`
- `processor/agentic-model/delivery_owner.go:50` — `func (c *Component) recordDeliveryOwnerFatal(result natsclient.DeliveryResult) {`
- `processor/agentic-governance/component.go:718` — `func (c *Component) recordDeliveryOwnerFatal(result natsclient.DeliveryResult) {`
- `processor/agentic-dispatch/component.go:163` — `type streamConsumerBinding struct {`
- `processor/agentic-tools/component.go:95` — `type streamConsumerBinding struct {`
- `processor/agentic-loop/component.go:117` — `type streamConsumerBinding struct {`
- `processor/agentic-model/component.go:103` — `type streamConsumerBinding struct {`
- `processor/agentic-governance/component.go:70` — `type streamConsumerBinding struct {`

Sizes: `wc -l` of the five `delivery_owner.go` files = 136 + 122 + 116 + 101 + 75 = **550**. Outside those files the
same shape also owns: governance's `drain` (`component.go:728-733`, 6 lines), governance's panic wrapper
(`component.go:349-361`, 13 lines), and five `streamConsumerBinding` struct declarations (5 lines each, 25). Total
spelled surface **594 lines**; the five `recordDeliveryOwnerFatal` bodies are component health writers, not latch
code, and are counted separately in the design.

No shared home exists (searches in § Searches: the name under every spelling, the two candidate directories).

Governance's copy is narrower **in the file only**: `consumeAdmittedDelivery` is genuinely absent, but its drain and its
panic wrapper exist and live in `component.go:728` and `component.go:349` — the issue's "no `drain` / run-wrapper" is
a wording defect, not a fact. Dispatch and tools carry `onRefused` and a `refuse` method; loop,
model and governance do not. Dispatch and loop carry the panic wrapper in the file; governance carries it in
`component.go`; tools and model have none (tools/model are heartbeat-only lanes; the heartbeat helper synthesizes
Quarantine on panic itself, `natsclient/delivery_settlement.go` D6). `observeDeliveryLane` takes a `portName` in
loop and governance only.

## 2. Every current spelling of the fact

The fact: *this lane admits no new local work after its first owner-stop result, and the exact consume handle is
drained at most once.* Its source of truth is one accessor on the settlement result; its five interpreters are the
copies in § 1. Two further spellings of PART of the fact exist on other planes — a drain-once around a native handle,
and a binding that records drain-issued without any admission latch. The second is spelled on ELEVEN planes (not the
three the first draft named): each retains a `jetstream.ConsumeContext`, a `drainIssued bool`, and exactly one
Stop-side guarded `handle.Drain()` (`grep -cE '\.Drain\(\)'` = 1 per file, § Measurements):

- `natsclient/delivery_settlement.go:263` — `func (r DeliveryResult) OwnerStopRequired() bool { return r.ownerStopNeeded }`
- `natsclient/client.go:757` — `drainOnce     sync.Once`
- `natsclient/client.go:788` — `s.drainOnce.Do(func() {`
- `processor/json_filter/json_filter.go:116` — `handle      jetstream.ConsumeContext`
- `processor/json_filter/json_filter.go:117` — `drainIssued bool`
- `processor/json_filter/json_filter.go:538` — `binding.handle.Drain()`
- `processor/json_generic/json_generic.go:100` — `handle      jetstream.ConsumeContext`
- `processor/json_generic/json_generic.go:101` — `drainIssued bool`
- `processor/json_generic/json_generic.go:493` — `binding.handle.Drain()`
- `processor/json_map/json_map.go:122` — `handle      jetstream.ConsumeContext`
- `processor/json_map/json_map.go:123` — `drainIssued bool`
- `processor/json_map/json_map.go:539` — `binding.handle.Drain()`
- `output/file/file.go:139` — `handle      jetstream.ConsumeContext`
- `output/file/file.go:140` — `drainIssued bool`
- `output/file/file.go:536` — `binding.handle.Drain()`
- `output/httppost/httppost.go:139` — `handle      jetstream.ConsumeContext`
- `output/httppost/httppost.go:140` — `drainIssued bool`
- `output/httppost/httppost.go:526` — `binding.handle.Drain()`
- `output/websocket/websocket.go:194` — `handle      jetstream.ConsumeContext`
- `output/websocket/websocket.go:195` — `drainIssued bool`
- `output/websocket/websocket.go:943` — `binding.handle.Drain()`
- `processor/graph-ingest/component.go:648` — `handle      jetstream.ConsumeContext`
- `processor/graph-ingest/component.go:649` — `drainIssued bool`
- `processor/graph-ingest/component.go:1128` — `binding.handle.Drain()`
- `processor/rule/processor.go:580` — `handle      jetstream.ConsumeContext`
- `processor/rule/processor.go:581` — `drainIssued bool`
- `processor/rule/processor.go:1351` — `consumers[i].handle.Drain()`
- `storage/objectstore/component.go:90` — `handle      jetstream.ConsumeContext`
- `storage/objectstore/component.go:91` — `drainIssued bool`
- `storage/objectstore/component.go:634` — `binding.handle.Drain()`
- `examples/processors/document/component.go:143` — `handle      jetstream.ConsumeContext`
- `examples/processors/document/component.go:144` — `drainIssued bool`
- `examples/processors/document/component.go:422` — `binding.handle.Drain()`
- `examples/processors/iot_sensor/component.go:142` — `handle      jetstream.ConsumeContext`
- `examples/processors/iot_sensor/component.go:143` — `drainIssued bool`
- `examples/processors/iot_sensor/component.go:422` — `binding.handle.Drain()`
- `agentic/agentrun/agentrun.go:812` — `handleErr := natsclient.ConsumeWithHeartbeat(msgCtx, msg, 10*time.Second, func(workCtx context.Context) error {`
- `processor/agentic-dispatch/component.go:345` — `status = "delivery ownership lost"`
- `processor/agentic-dispatch/component.go:347` — `status = "terminal delivery ownership lost"`
- `processor/agentic-governance/component.go:703` — `status = "delivery ownership lost"`
- `processor/agentic-loop/component.go:471` — `status = "delivery ownership lost"`
- `processor/agentic-model/component.go:1184` — `return "delivery ownership lost"`
- `processor/agentic-tools/component.go:1441` — `return "delivery ownership lost"`

The sixth spelling is drafted, not landed: `<scratchpad>/gh1249/design-draft-4.md:85-92` (§ 2.6) copies loop
`delivery_owner.go:29-63` and `:74-86` verbatim into a new `agentic/agentrun/delivery_owner.go` (≈75 lines, § 7 of that
draft) and gives `milestoneConsumerOwner` (`agentrun.go:679`) a per-lane admission plus observer that drains under
`o.mu` and sets that lane's drained flag — explicitly "No second `drainOnce`". `agentrun.go:812` today calls the
legacy `ConsumeWithHeartbeat`; its migration is the #1249 layer. The L4 draft (`<scratchpad>/l4/change/design.md:217`)
lists `delivery_owner.go` under "Survives" — L4 edits the loop copy's neighbourhood (`component.go` setupConsumer and
handlers), not the copy.

The status string `delivery ownership lost` is spelled five times (six with dispatch's terminal variant) — it is the
health projection of the latched fact, written by each `recordDeliveryOwnerFatal`, and stays with the component.

The eleven `drainIssued` bindings hold a handle but no admission and have exactly one drainer (Stop); a non-atomic
bool is sound there and a `sync.Once` would solve a problem they do not have. The class boundary is "has a concurrent
drainer": the five agentic bindings need drain-once because the observer goroutine is a second drainer. The eleven
are not adopters of the fact until they take typed settlement and an observer (adoption sweep, § 5).

## Adjacent claims on the territory (category 3)

Current spec text and two landed design records constrain the hoist directly. Conflicts are named, not designed
around.

**Conflict A — a current-spec THEN clause forbids the shape literally.** `jetstream-consumer-policy` says a shared
helper owns no admission. L1 has since archived, so its delta is synced into the live spec and the clause is now LIVE
twice (`:655` under "shared settlement remains stateless and heartbeat-specific", `:681` under "settlement-only
delivery decisions use one shared interpreter"); the archived delta pins below are kept as the provenance of the
second one. The hoist needs three MODIFIED requirements, not a footnote — which is what this change's delta carries,
and all three now validate against the live spec rather than against an unlanded delta.

- `openspec/specs/jetstream-consumer-policy/spec.md:599` — `### Requirement: control loss shuts down through the existing exact owner`
- `openspec/specs/jetstream-consumer-policy/spec.md:601` — `Each migrated physical binding SHALL create private admission before acquisition and buffer its first`
- `openspec/specs/jetstream-consumer-policy/spec.md:602` — `OwnerStopRequired result. Closed admission SHALL perform no work, heartbeat, or terminal method. Already-admitted work`
- `openspec/specs/jetstream-consumer-policy/spec.md:606` — `Refusal by closed admission is a declared event, not a silent drop. Each refused delivery SHALL emit a substrate log`
- `openspec/specs/jetstream-consumer-policy/spec.md:607` — `line and increment a lane-labelled counter naming the refusal and why continuing is safe (ADR-098). The buffered`
- `openspec/specs/jetstream-consumer-policy/spec.md:641` — `### Requirement: shared settlement remains stateless and heartbeat-specific`
- `openspec/specs/jetstream-consumer-policy/spec.md:655` — `- **AND** no shared helper owns admission, a native handle, health, shutdown, or restart`
- `openspec/specs/jetstream-consumer-policy/spec.md:664` — `### Requirement: settlement-only delivery decisions use one shared interpreter`
- `openspec/specs/jetstream-consumer-policy/spec.md:681` — `- **AND** no shared helper owns admission, a native handle, health, shutdown, or restart`
- `openspec/changes/archive/2026-09-19-settle-after-durable-effect/specs/jetstream-consumer-policy/spec.md:22` — `- **AND** no shared helper owns admission, a native handle, health, shutdown, or restart`
- `openspec/changes/archive/2026-09-19-settle-after-durable-effect/specs/jetstream-consumer-policy/spec.md:52` — `- **AND** no shared helper owns admission, a native handle, health, shutdown, or restart`
- `openspec/changes/archive/2026-09-19-settle-after-durable-effect/specs/agentic-loop/spec.md:101` — `- **AND** the latch itself adds no metric family, public state, durable state, or communication path`

**Conflict B — the archived settlement design (landed as #1331) ruled "Shared natsclient owns no lifecycle" and
rejected a shared gate/supervisor.** A package that constructs a latch and a drain-once binding the OWNER retains is
not a supervisor and owns no lifecycle; but placing it in `natsclient` reads against D8, so home matters.

- `openspec/changes/archive/2026-09-19-semantic-jetstream-settlement/design.md:223` — `Each migrated physical binding privately creates admission and a capacity-one first-fatal signal before acquisition.`
- `openspec/changes/archive/2026-09-19-semantic-jetstream-settlement/design.md:229` — `The observer derives from Start/Run and joins Stop. Shared natsclient owns no lifecycle.`
- `openspec/changes/archive/2026-09-19-semantic-jetstream-settlement/design.md:333` — `- Shared gate/supervisor/durable quarantine: duplicates component and JetStream authority.`
- `docs/adr/095-one-shot-running-lifecycle-with-retained-failed-start-cleanup-authority.md:21` — `different responsibilities. Combining them in a Client catalog or stateful wrapper hides duplicate ownership and asks`
- `docs/adr/095-one-shot-running-lifecycle-with-retained-failed-start-cleanup-authority.md:26` — `Running owners retain the exact native `jetstream.ConsumeContext` returned at the delivery commit point. All fallible`
- `openspec/specs/jetstream-consumer-policy/spec.md:303` — `### Requirement: Client owns no consumer or subscription children`
- `openspec/specs/nats-streaming/spec.md:166` — `every `DeliveryResult`, and stops the exact retained consumer owner outside the callback when `OwnerStopRequired` is`
- `openspec/specs/component-lifecycle/spec.md:11` — `join, and cleanup sequence. The exact resource-specific ordering MUST preserve admitted callback authority where its`
- `openspec/specs/component-lifecycle/spec.md:12` — `native drain protocol requires it; no universal cancel-before-drain order is implied.`
- `docs/concepts/33-semantic-settlement.md:89` — `4. Inspect every `DeliveryResult`; when `OwnerStopRequired` is true, close admission and stop that exact handle outside`
- `docs/operations/migration-restart-safe-nats-client.md:87` — `and settlement evidence in existing health/log surfaces. If `OwnerStopRequired` is true, close admission and stop the`
- `docs/operations/migration-restart-safe-nats-client.md:88` — `exact retained consume handle outside the callback. A terminal-method error alone does not authorize owner shutdown.`
- `openspec/changes/archive/2026-09-19-settle-after-durable-effect/design.md:328` — `- The delivery-owner trio (`deliveryLaneAdmission`, `newStreamConsumerBinding`, `observeDeliveryLane`) is now`
- `openspec/changes/archive/2026-09-19-settle-after-durable-effect/design.md:330` — `the inline locals that made it a fifth spelling. Hoisting the trio into a shared internal package would move`
- `openspec/changes/archive/2026-09-19-settle-after-durable-effect/design.md:331` — `~400 lines across four components and is far past the bound this round set for the refactor, so the duplication`
- `openspec/changes/archive/2026-09-19-settle-after-durable-effect/design.md:332` — `is recorded, not removed.`
- `release/tier1-packages.txt:57` — `github.com/c360studio/semstreams/natsclient`
- `release/tier1-packages.txt:39` — `github.com/c360studio/semstreams/agentic`
- `release/tier1-packages.txt:40` — `github.com/c360studio/semstreams/agentic/agentrun`
- `release/tier1-packages.txt:74` — `github.com/c360studio/semstreams/processor/agentic-dispatch`
- `release/tier1-packages.txt:75` — `github.com/c360studio/semstreams/processor/agentic-loop`
- `release/tier1-packages.txt:77` — `github.com/c360studio/semstreams/processor/agentic-model`
- `release/tier1-packages.txt:78` — `github.com/c360studio/semstreams/processor/agentic-tools`
- `docs/adr/106-beta-rc-v1-exit-criteria-and-the-two-tier-surface-freeze.md:50` — `- **Tier 1 — frozen, semver-binding at 1.0.** The Go packages any sister imports (`release/tier1-packages.txt`,`
- `docs/adr/106-beta-rc-v1-exit-criteria-and-the-two-tier-surface-freeze.md:53` — `- **Tier 2 — explicitly not frozen.** The remaining ~91 non-internal packages, to be moved under `internal/` or`

**Tier consequence (ADR-106).** `natsclient`, `agentic`, `agentic/agentrun`, and the four agentic processors are Tier 1.
A new package under `internal/` is outside every tier; a new package under `pkg/` or a new symbol in `natsclient` is
a Tier 1 widening (compatible addition, walked-path guard RC-6). Deleting five UNEXPORTED types from Tier 1 processor
packages is invisible to `scripts/api-compat.sh` (it diffs exported API).

**Current-truth gap found while measuring, out of scope, recorded (re-measured at `3faca84f`):** the spec at
`:605-607` says each refused delivery is declared (log line + lane-labelled counter). Of the eight lanes here only
three declare: dispatch `agent.complete` (`component.go:618-620`), dispatch `agent.failed` (`:659-661`), tools
`tool.execute` (`:434-436`). Dispatch's `user.message` lane (`:577`), both loop lanes (`:1098`, `:1120`), model
(`:408`) and every governance port (`:509`) refuse silently — five silent lanes, which is the count #1342 carries.
Not a #1341 behavior change; a shared shape makes closing it a one-argument edit per lane.

Issues and drafts on the territory (non-file entries):

- #1341 — this change; owner ruling 2026-09-19 quoted in the problem statement
- #1249 — will consume the package (draft-4 § 2.6 copies loop `:29-63`, `:74-86`; adds `DeliveryFatal()` and `RegisterMetrics`)
- #1330 — L4; its draft leaves `delivery_owner.go` untouched ("Survives") and edits the loop copy's neighbourhood
- #1329 — L3, PR #1338 (draft, base `claude/gh1328-stable-identity`, head `f4fd4369` on the pre-rebase line)
- #1327 — L1, PR #1334 (draft, head `20fe8d09` = this base)
- #759 — the settlement program; D8 and the rejected "shared gate/supervisor" are its rulings
- #1342 — FILED 2026-09-19 from this inventory's R1: five lanes at `c58c65bd` refuse silently against spec `:607-609`; blocked by #1341

## 4. The consumer at birth

Every symbol the package would export has present in-tree consumers: the lane setup sites, the Stop paths that drain
and join, and the tests that reach the latch. Enumerated so zero-consumer surface cannot enter the design. Lane
setup sites (admission construction, admitted consume or settle, binding, observe):

- `processor/agentic-dispatch/component.go:577` — `userMessageAdmission := newDeliveryLaneAdmission(c.recordDeliveryOwnerFatal, nil)`
- `processor/agentic-dispatch/component.go:579` — `if !userMessageAdmission.admit() {`
- `processor/agentic-dispatch/component.go:582` — `decision, cause := runDispatchDeliveryWork(msgCtx, msg.Data(), c.handleUserMessage)`
- `processor/agentic-dispatch/component.go:583` — `result := natsclient.SettleDelivery(msg, decision, cause)`
- `processor/agentic-dispatch/component.go:584` — `userMessageAdmission.latch(result)`
- `processor/agentic-dispatch/component.go:592` — `userMessageBinding := newStreamConsumerBinding(handle)`
- `processor/agentic-dispatch/component.go:593` — `c.observeDeliveryLane(ctx, &userMessageBinding, userMessageAdmission)`
- `processor/agentic-dispatch/component.go:618` — `agentCompleteAdmission := newDeliveryLaneAdmission(c.recordAgentCompleteFatal, func(subject string) {`
- `processor/agentic-dispatch/component.go:619` — `c.recordDeliveryRefused(bindings.agentComplete.portName, subject)`
- `processor/agentic-dispatch/component.go:622` — `result, admitted := consumeAdmittedDelivery(msgCtx, msg, agentCompletePolicy, agentCompleteAdmission)`
- `processor/agentic-dispatch/component.go:633` — `agentCompleteBinding := newStreamConsumerBinding(handle)`
- `processor/agentic-dispatch/component.go:634` — `c.observeDeliveryLane(ctx, &agentCompleteBinding, agentCompleteAdmission)`
- `processor/agentic-dispatch/component.go:659` — `agentFailedAdmission := newDeliveryLaneAdmission(c.recordAgentFailedFatal, func(subject string) {`
- `processor/agentic-dispatch/component.go:663` — `result, admitted := consumeAdmittedDelivery(msgCtx, msg, agentFailedPolicy, agentFailedAdmission)`
- `processor/agentic-dispatch/component.go:674` — `agentFailedBinding := newStreamConsumerBinding(handle)`
- `processor/agentic-dispatch/component.go:675` — `c.observeDeliveryLane(ctx, &agentFailedBinding, agentFailedAdmission)`
- `processor/agentic-tools/component.go:434` — `admission := newDeliveryLaneAdmission(c.recordDeliveryOwnerFatal, func(subject string) {`
- `processor/agentic-tools/component.go:435` — `c.recordDeliveryRefused(lane, subject)`
- `processor/agentic-tools/component.go:449` — `result, admitted := consumeAdmittedDelivery(msgCtx, msg, deliveryPolicy, admission)`
- `processor/agentic-tools/component.go:459` — `binding := newStreamConsumerBinding(handle)`
- `processor/agentic-tools/component.go:460` — `c.observeDeliveryLane(ctx, &binding, admission)`
- `processor/agentic-loop/component.go:1098` — `admission = newDeliveryLaneAdmission(c.recordDeliveryOwnerFatal)`
- `processor/agentic-loop/component.go:1100` — `result, admitted := consumeAdmittedDelivery(msgCtx, msg, policy, admission)`
- `processor/agentic-loop/component.go:1120` — `admission = newDeliveryLaneAdmission(c.recordDeliveryOwnerFatal)`
- `processor/agentic-loop/component.go:1125` — `decision, cause := runLoopDeliveryWork(msgCtx, msg.Data(), settleHandlerFn)`
- `processor/agentic-loop/component.go:1126` — `result := natsclient.SettleDeliveryWithRetry(msg, settleRetry, decision, cause)`
- `processor/agentic-loop/component.go:1127` — `admission.latch(result)`
- `processor/agentic-loop/component.go:1143` — `binding := newStreamConsumerBinding(handle)`
- `processor/agentic-loop/component.go:1145` — `c.observeDeliveryLane(consumerCtx, &binding, admission, port.Name)`
- `processor/agentic-model/component.go:408` — `admission := newDeliveryLaneAdmission(c.recordDeliveryOwnerFatal)`
- `processor/agentic-model/component.go:415` — `result, admitted := consumeAdmittedDelivery(msgCtx, msg, policy, admission)`
- `processor/agentic-model/component.go:424` — `binding := newStreamConsumerBinding(handle)`
- `processor/agentic-model/component.go:425` — `c.observeDeliveryLane(ctx, &binding, admission)`
- `processor/agentic-governance/component.go:509` — `admission := newDeliveryLaneAdmission(c.recordDeliveryOwnerFatal)`
- `processor/agentic-governance/component.go:514` — `decision, cause := runGovernanceDeliveryWork(msgCtx, msg.Data(), handler)`
- `processor/agentic-governance/component.go:515` — `result := natsclient.SettleDelivery(msg, decision, cause)`
- `processor/agentic-governance/component.go:516` — `admission.latch(result)`
- `processor/agentic-governance/component.go:524` — `binding := newStreamConsumerBinding(handle)`
- `processor/agentic-governance/component.go:525` — `c.observeDeliveryLane(ctx, &binding, admission, port.Name)`

Stop paths that drain the exact handle, await `Closed()`, and join the observer (`observerDone`):

- `processor/agentic-dispatch/component.go:505` — `binding.drain()`
- `processor/agentic-dispatch/component.go:506` — `stopErr = errors.Join(stopErr, c.awaitConsumerClosed(ctx, binding.handle.Closed()))`
- `processor/agentic-dispatch/component.go:513` — `if done := c.consumers[i].observerDone; done != nil {`
- `processor/agentic-tools/component.go:672` — `binding.drain()`
- `processor/agentic-tools/component.go:673` — `closed := binding.handle.Closed()`
- `processor/agentic-tools/component.go:688` — `if done := c.consumers[i].observerDone; done != nil {`
- `processor/agentic-loop/component.go:766` — `binding.drain()`
- `processor/agentic-loop/component.go:767` — `closed := binding.handle.Closed()`
- `processor/agentic-loop/component.go:785` — `if observerDone := c.consumers[i].observerDone; observerDone != nil {`
- `processor/agentic-model/component.go:541` — `binding.drain()`
- `processor/agentic-model/component.go:542` — `closed := binding.handle.Closed()`
- `processor/agentic-model/component.go:557` — `if done := c.consumers[i].observerDone; done != nil {`
- `processor/agentic-governance/component.go:634` — `binding.drain()`
- `processor/agentic-governance/component.go:635` — `closed := binding.handle.Closed()`
- `processor/agentic-governance/component.go:650` — `if done := c.consumers[i].observerDone; done != nil {`
- `processor/agentic-dispatch/component.go:123` — `consumers []streamConsumerBinding`
- `processor/agentic-tools/component.go:84` — `consumers           []streamConsumerBinding`
- `processor/agentic-loop/component.go:76` — `consumers     []streamConsumerBinding`
- `processor/agentic-model/component.go:64` — `consumers          []streamConsumerBinding`
- `processor/agentic-governance/component.go:57` — `consumers          []streamConsumerBinding`

Component-side callbacks the shape composes (fatal recorders, refusal declarers, fatal-side observers):

- `processor/agentic-dispatch/component.go:683` — `func (c *Component) observeTerminalDelivery(err error) {`
- `processor/agentic-dispatch/component.go:695` — `func (c *Component) recordDeliveryRefused(lane, subject string) {`
- `processor/agentic-dispatch/component.go:697` — `c.metrics.recordDeliveryRefused(lane)`
- `processor/agentic-dispatch/component.go:713` — `}`
- `processor/agentic-dispatch/component.go:721` — `}`
- `processor/agentic-dispatch/component.go:735` — `if c.deliveryFatalErr == nil {`
- `processor/agentic-dispatch/component.go:736` — `c.deliveryFatalErr = result.Err()`
- `processor/agentic-tools/component.go:485` — `func (c *Component) recordHandlerError(ctx context.Context, err error) {`
- `processor/agentic-tools/component.go:508` — `func (c *Component) recordDeliveryRefused(lane, subject string) {`
- `processor/agentic-tools/component.go:510` — `c.metrics.recordDeliveryRefused(lane)`
- `processor/agentic-tools/component.go:525` — `if c.deliveryFatalErr != nil {`
- `processor/agentic-tools/component.go:528` — `c.deliveryFatalErr = err`
- `processor/agentic-tools/component.go:529` — `c.errors++`
- `processor/agentic-loop/delivery_owner.go:68` — `if c.deliveryFatalErr != nil {`
- `processor/agentic-loop/delivery_owner.go:71` — `c.deliveryFatalErr = result.Err()`
- `processor/agentic-model/delivery_owner.go:53` — `if c.deliveryFatalErr != nil {`
- `processor/agentic-model/delivery_owner.go:56` — `c.deliveryFatalErr = result.Err()`
- `processor/agentic-model/delivery_owner.go:57` — `c.errors++`
- `processor/agentic-governance/component.go:721` — `if c.deliveryFatalErr != nil {`
- `processor/agentic-governance/component.go:724` — `c.deliveryFatalErr = result.Err()`
- `processor/agentic-governance/component.go:725` — `atomic.AddInt64(&c.errors, 1)`
- `processor/agentic-dispatch/delivery_owner.go:130` — `c.observeTerminalDelivery(result.Err())`
- `processor/agentic-dispatch/delivery_owner.go:131` — `c.logger.Error("Terminal delivery ownership lost", slog.Any("error", result.Err()))`
- `processor/agentic-tools/delivery_owner.go:117` — `c.recordHandlerError(ctx, result.Err())`
- `processor/agentic-loop/delivery_owner.go:111` — `c.logger.Error("Loop delivery ownership lost", "port", portName, "error", result.Err())`
- `processor/agentic-model/delivery_owner.go:96` — `c.logger.Error("Model delivery ownership lost", "error", result.Err())`
- `processor/agentic-governance/delivery_owner.go:70` — `c.logger.Error("Governance delivery ownership lost", "port", portName, "error", result.Err())`
- `processor/agentic-dispatch/metrics.go:339` — `func (m *routerMetrics) recordDeliveryRefused(lane string) {`
- `processor/agentic-tools/metrics.go:132` — `func (m *toolsMetrics) recordDeliveryRefused(lane string) {`

**Test census, generated mechanically and REGENERATED at the re-pin base** (`git grep -lE` for the six symbols in § 1 plus `streamConsumerBinding`, `observerDone`, `admission.(fatal|admit|latch|refuse)`, `.handle.Closed` over `processor/agentic-*/*_test.go`, each hit attributed to its enclosing `func Test`): at `20fe8d09` this was 36 test functions across 16 files; at `3faca84f` it is **45 test functions across 23 files, 5 of them `//go:build integration`** (`agentic-dispatch/terminal_settlement_integration_test.go`, `agentic-governance/delivery_settlement_integration_test.go`, `agentic-loop/delivery_settlement_integration_test.go`, `agentic-loop/partial_publish_settlement_integration_test.go`, `agentic-loop/terminal_failure_record_integration_test.go`). L2 and L3 added seven test files that touch these symbols, six of them under `agentic-loop`, and one integration-tagged file the per-component gate in `tasks.md` did not name. The five `lifecycle_causal_test.go` files construct a binding by literal (`[]streamConsumerBinding{{handle: h}}`, nil `drainOnce`, nil `observerDone`) and are the do-nothing path a pointer `Binding` with a never-nil `Done()` must keep working. One pin per test, at its first reference:

- `processor/agentic-dispatch/delivery_owner_test.go:73` — `<-binding.observerDone`
- `processor/agentic-dispatch/delivery_owner_test.go:153` — `admission := newDeliveryLaneAdmission(c.recordAgentCompleteFatal, nil)`
- `processor/agentic-dispatch/delivery_owner_test.go:193` — `admission := newDeliveryLaneAdmission(func(result natsclient.DeliveryResult) { tt.record(c, result) }, nil)`
- `processor/agentic-dispatch/delivery_owner_test.go:212` — `newDeliveryLaneAdmission(c.recordDeliveryOwnerFatal, nil).latch(result)`
- `processor/agentic-dispatch/delivery_owner_test.go:235` — `admission := newDeliveryLaneAdmission(c.recordAgentCompleteFatal, func(subject string) {`
- `processor/agentic-dispatch/delivery_owner_test.go:302` — `if binding.observerDone != nil {`
- `processor/agentic-dispatch/lifecycle_causal_test.go:150` — `c := &Component{lifecycleUsed: true, started: true, cancel: cancelRun, consumers: []streamConsumerBinding{{handle: h}}}`
- `processor/agentic-dispatch/terminal_settlement_integration_test.go:454` — `case <-c.consumers[1].handle.Closed():`
- `processor/agentic-dispatch/terminal_settlement_integration_test.go:498` — `completeClosed := c.consumers[1].handle.Closed()`
- `processor/agentic-governance/delivery_settlement_integration_test.go:82` — `<-binding.observerDone`
- `processor/agentic-governance/delivery_settlement_test.go:55` — `<-binding.observerDone`
- `processor/agentic-governance/delivery_settlement_test.go:148` — `<-binding.observerDone`
- `processor/agentic-governance/lifecycle_causal_test.go:136` — `c := &Component{lifecycleUsed: true, running: true, cancel: cancelRun, consumers: []streamConsumerBinding{{handle: h}}}`
- `processor/agentic-loop/carried_continuation_durability_test.go:38` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/delivery_owner_test.go:158` — `admission := newDeliveryLaneAdmission(nil)`
- `processor/agentic-loop/delivery_owner_test.go:231` — `admission := newDeliveryLaneAdmission(nil)`
- `processor/agentic-loop/delivery_owner_test.go:339` — `<-binding.observerDone`
- `processor/agentic-loop/delivery_owner_test.go:388` — `if binding.observerDone != nil {`
- `processor/agentic-loop/delivery_owner_test.go:452` — `<-binding.observerDone`
- `processor/agentic-loop/delivery_owner_test.go:502` — `<-binding.observerDone`
- `processor/agentic-loop/delivery_settlement_integration_test.go:113` — `<-binding.observerDone`
- `processor/agentic-loop/delivery_settlement_integration_test.go:194` — `<-binding.observerDone`
- `processor/agentic-loop/lifecycle_causal_test.go:204` — `c := &Component{lifecycleUsed: true, started: true, cancel: cancelRun, consumers: []streamConsumerBinding{{handle: h}}}`
- `processor/agentic-loop/missing_loop_settlement_test.go:92` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/missing_loop_settlement_test.go:112` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/missing_loop_settlement_test.go:148` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/missing_loop_settlement_test.go:211` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/partial_publish_settlement_integration_test.go:61` — `admission := newDeliveryLaneAdmission(func(r natsclient.DeliveryResult) {`
- `processor/agentic-loop/publish_phase_fatal_test.go:98` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/response_handler_failure_test.go:61` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/superseded_response_test.go:93` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/superseded_response_test.go:185` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/task_identity_conflict_test.go:54` — `result, admitted := consumeAdmittedDelivery(t.Context(), msg,`
- `processor/agentic-loop/terminal_failure_record_integration_test.go:75` — `result, admitted := consumeAdmittedDelivery(t.Context(), msg,`
- `processor/agentic-loop/tool_result_handler_failure_test.go:69` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-loop/tool_result_handler_failure_test.go:209` — `result, admitted := consumeAdmittedDelivery(`
- `processor/agentic-model/delivery_owner_test.go:61` — `admission := newDeliveryLaneAdmission(nil)`
- `processor/agentic-model/delivery_owner_test.go:161` — `<-binding.observerDone`
- `processor/agentic-model/lifecycle_causal_test.go:138` — `c := &Component{lifecycleUsed: true, running: true, cancel: cancelRun, consumers: []streamConsumerBinding{{handle: h}}, clientCache: map[string]*Client{}}`
- `processor/agentic-tools/delivery_owner_test.go:85` — `admission := newDeliveryLaneAdmission(component.recordDeliveryOwnerFatal, func(subject string) {`
- `processor/agentic-tools/delivery_owner_test.go:137` — `admission := newDeliveryLaneAdmission(nil, nil)`
- `processor/agentic-tools/delivery_owner_test.go:182` — `admission := newDeliveryLaneAdmission(nil, nil)`
- `processor/agentic-tools/delivery_owner_test.go:209` — `admission := newDeliveryLaneAdmission(nil, nil)`
- `processor/agentic-tools/delivery_owner_test.go:245` — `if binding.observerDone != nil {`
- `processor/agentic-tools/lifecycle_causal_test.go:187` — `c := &Component{lifecycleUsed: true, running: true, cancel: cancelRun, consumers: []streamConsumerBinding{{handle: h}}}`

Outside this repository, present consumers of an EXPORTED form: **zero**. The nine sisters were probed read-only (§ Searches): none calls `ConsumeDeliveryWithHeartbeat`, `SettleDelivery`, `OwnerStopRequired`, or `DeliveryResult`. semdev calls the legacy `ConsumeWithHeartbeat` at `internal/conversationchannel/component.go:476` and `internal/intake/component.go:378` (semdev `ca3956a`); the #1249 layer deletes that helper with no deprecation, so semdev is a FUTURE adopter of the latch shape, not a present one.

## 5. The problem shape

Independently of the fact: *latch-closed admission at a callback seam, a drain-once around a native handle, and a
one-shot observer that reacts to the first fatal signal by draining that exact handle, joined by Stop.* Closest
existing instances on any plane, and whether the design adopts them:

- `natsclient/client.go:753` — `// Subscription wraps a NATS subscription for lifecycle management.`
- `natsclient/client.go:755` — `sub nativeSubscription`
- `natsclient/client.go:757` — `drainOnce     sync.Once`
- `processor/graph-ingest/authority_gate.go:51` — `func (c *Component) authorizeSubject(subject string, importLane bool) error {`
- `pkg/fusion/contract.go:331` — `func (b *budgeter) admit(bodyBytes int) bool {`
- `internal/lifecyclecleanup/lifecyclecleanup.go:1` — `// Package lifecyclecleanup contains the one stateless policy shared by`
- `internal/lifecyclecleanup/lifecyclecleanup.go:2` — `// lifecycle owners when Start fails after acquiring cleanup obligations.`
- `internal/maxdelivery/observer.go:1` — `// Package maxdelivery owns SemStreams' durable visibility for JetStream`
- `internal/maxdelivery/observer.go:4` — `// It is internal because this is framework boot plumbing, not a component or an`
- `internal/maxdelivery/observer.go:22` — `"github.com/c360studio/semstreams/natsclient"`
- `internal/componentadmission/access.go:1` — `// Package componentadmission gates framework-internal component admission`
- `internal/componentadmission/access.go:6` — `// lifecycle coordination. The Go internal-package rule prevents downstream`
- `internal/componentadmission/access.go:7` — `// adopters from importing this capability.`

`natsclient.Subscription` (`client.go:753-760`) is the same drain-once-around-a-native-handle idea for a core NATS
subscription: a shared type holds the native handle and a `sync.Once`, while the OWNER decides when to drain. It is
the precedent that a shared drain-once wrapper is compatible with owner-retained lifecycle authority (ADR-095 `:26`).
Not adoptable as-is: it wraps `nativeSubscription`, not `jetstream.ConsumeContext`, and it awaits closure itself,
which the five Stop paths do in the component under their own context.

`authority_gate.go:51` is admit-or-refuse with a classified refusal per delivery — a different shape (per-item
decision, no latch); it is not adopted, but its "one home for the refusal metric mapping" rule (`:55-58`) is the
reason the refusal declaration stays a caller-supplied callback rather than a package-owned counter.

`internal/lifecyclecleanup` ("the one stateless policy shared by lifecycle owners") and `internal/maxdelivery` (an
internal package that imports `natsclient` and `jetstream`) are the two precedents for the HOME: a framework-internal
package, importable by every in-module owner including `agentic/agentrun`, invisible to adopters, importing
`natsclient` without creating a cycle (`natsclient` imports nothing under `internal/`; measured in § Searches).

**Adoption sweep (establishing side; enumeration only, never migration).** This change establishes a named primitive.
Planes that should adopt it, one line each: `agentic/agentrun/agentrun.go:812` (the #1249 layer, already sequenced
behind this change); the eleven `drainIssued` bindings pinned in § 2 — `output/file/file.go:139`,
`output/httppost/httppost.go:139`, `output/websocket/websocket.go:194`, `processor/graph-ingest/component.go:648`,
`processor/rule/processor.go:580`, `storage/objectstore/component.go:90`, `processor/json_filter/json_filter.go:116`,
`processor/json_generic/json_generic.go:100`, `processor/json_map/json_map.go:122`,
`examples/processors/document/component.go:143`, `examples/processors/iot_sensor/component.go:142` — each with one
Stop-side drainer and no observer, so an adopter only when it takes typed settlement and a concurrent drainer (none
is sequenced); semdev's two legacy call sites (a sister; recorded, never edited here). Twelve candidate planes in
this module, not four. Enumeration only: none is migrated here and the count holds nothing.
No in-tree plane beyond those four calls typed settlement (`git grep` for `natsclient.ConsumeDeliveryWithHeartbeat`
and `natsclient.SettleDelivery` outside `natsclient/` returns only the five agentic components and one comment in
`component/port_jetstream.go:43`).

## Same-class collision table

Triggered: the proposal introduces a runtime-coordination primitive. The semantic job is stated first; every cell is
cited or closed by a search.

| Dimension | Evidence |
|---|---|
| Semantic class | Per-lane admission that closes on the first `OwnerStopRequired` result; buffering that result for an owner-side observer; draining the exact `jetstream.ConsumeContext` at most once, whether from the observer or from Stop. Source of the fact: `natsclient/delivery_settlement.go:263`. |
| Owners | The five agentic components (§ 1); each lane owns one latch and one binding; the component owns the handle, health, Stop. No package-level owner exists (§ Searches). |
| Catalogs | None. No registry, descriptor, or config key names a lane latch (`git grep -n -i 'admission' -- config/ schemas/ specs/` returns only `componentadmission` and `max_ack_pending` policy text). |
| Status | Health only: `deliveryFatalErr` on each component (dispatch `component.go:112`, tools `:75`, loop `:95`, model `:75`, governance `:66`); dispatch's `agentCompleteFatal`/`agentFailedFatal` (`:110-111`); status strings at § 2. The latch itself has no status surface (L1 agentic-loop delta `:79`). |
| Lifecycle | Created before acquisition (`consume(...)` at each site in § 4); observer starts on the Start-derived context; Stop drains, awaits `Closed()`, cancels, joins `observerDone` (§ 4). No replay, repair, reset, or expiry — a latched lane is rebuilt only by explicit reconstruction (`agentic-dispatch/component.go:773`, `agentic-tools/component.go:517`). |
| Ownership | One latch per lane, never per component (L1 design `:33`); dispatch's terminal lanes latch into per-lane health fields, its other three share one field (`component.go:104-112`). Value-copied binding with a pointer `sync.Once` so slice copies share the once (`delivery_owner.go` binding ctors, § 1). |
| Readers | `Health()` (status sites, § 2); Stop paths (§ 4); tests (§ 4). No gateway or diagnostic reader (`git grep -n 'deliveryFatalErr' -- gateway/ service/ health/` → none). |
| Writers | `recordDeliveryOwnerFatal` ×5, `recordAgentCompleteFatal`, `recordAgentFailedFatal` (§ 4); the observer's drain; Stop's drain. No provisioning or recovery writer. |
| Recovery | None by design: after a fatal the lane stays drained and deliveries stay pending for redelivery to a reconstructed owner (`agentic-dispatch/component.go:773`). The package must not add one (archived design `:333`). |

## Adopter seam inventory

Run because the package's home decides whether a surface is reached from outside. Answered as a component author in
a sister repo who has never opened `delivery_owner.go` and is migrating off `ConsumeWithHeartbeat` because the #1249
layer deletes it (`docs/operations/migration-beta162-to-beta163.md:1146-1151`).

**1. What must they know today (typed API, no package)?** Six facts: (a) construct the latch before `Consume` so a
fatal reported before the handle returns is buffered, not lost (spec `:602`); (b) latch only on `OwnerStopRequired`,
never on a terminal-method error (`:589-590`); (c) closed admission runs no work, heartbeat, or terminal method
(`:603`); (d) drain the exact handle outside the callback, never inside it (`:604-605`); (e) join the observer during
Stop (`:605`); (f) declare each refusal with a log line and a lane-labelled counter (`:606-608`). Six is a design
finding — more than two.

**2. What happens if they do nothing?** The callback keeps running work after heartbeat control is lost: duplicate
effects on redelivery, no health signal, the handle never drained. Silent. The migration note (`:87-88`) tells them
to "close admission and stop the exact retained consume handle" and hands them nothing to do it with.

**3. Where do they find out?** Doc (`docs/concepts/33-semantic-settlement.md:89`; the migration note) → a finding for
a correctness fact. There is no compile, boot, or typed-runtime signal that a lane consumed without a latch.

**4. What should they have to know?** Nothing beyond "consume under an admission": construct one, pass it, retain the
binding. That is what the in-tree package gives the five owners. Whether it is exported is the one open question
this inventory surfaces for the owner: present external consumers are zero (§ 4), `openspec/project.md` admits a
package on two independent product reuses or a recorded usability gap, and semdev's forced migration is exactly
such a gap — but a future one.

**Prefer observation to prediction.** The typed API already observes the fatal (the framework decides
`OwnerStopRequired`); what it asks the adopter to build is the REACTION. The package is the framework absorbing that
reaction; no value is predicted by the caller.

Surfaces this change touches that ARE reached from outside: none if the home is `internal/`. If the home is `natsclient`
or `pkg/`, the exported symbols are a Tier 1 widening and `scripts/api-compat.sh` reports them under ADDED (RC-6
walked path owed).

## Measurements

Rows above the rule were recorded at the design base `20fe8d09` in worktree `verify-20fe8d09` and are kept as
provenance; rows below it are the round-3 re-measure at `3faca84f` in worktree `claude/gh1341-deliverylane`.

| Claim | Command | Result |
|---|---|---|
| Five copies, 550 lines | `wc -l processor/agentic-{dispatch,tools,loop,model,governance}/delivery_owner.go` | 136, 122, 116, 101, 75 = 550 |
| Dispatch vs tools differ by 24 lines | `diff <(cat processor/agentic-dispatch/delivery_owner.go) <(cat processor/agentic-tools/delivery_owner.go) \| grep -c '^[<>]'` | issue's figure; the differing hunks are the panic wrapper (13), imports (2), and the observer's fatal arm (2) |
| Governance's binding and wrapper live outside the file | `git grep -n 'func (b \*streamConsumerBinding) drain\|func runGovernanceDeliveryWork' -- processor/agentic-governance/` | `component.go:728`, `component.go:349` |
| L3 head not on this line | `git merge-base --is-ancestor 20fe8d09 f4fd4369; echo $?` | 1 (not ancestor); merge-base `4eeaf568` |
| L3 differs on the five files | `git diff --stat 20fe8d09 f4fd4369 -- 'processor/*/delivery_owner.go'` | dispatch 45, governance 75 (absent), tools 45 |
| main carries two copies | `git ls-tree --name-only f4d66934 -- processor/agentic-*/delivery_owner.go` | dispatch, tools |
| Tree builds | `go build ./...` | BUILD_OK |
| No cycle risk | `git grep -h '^\s*"github.com/c360studio/semstreams/' -- 'natsclient/*.go' ':!natsclient/*_test.go' \| sort -u` | metric, pkg/cache, pkg/errs, pkg/resource, pkg/retry — nothing under `internal/` or `processor/` |
| Internal precedent imports natsclient | `git grep -l '"github.com/c360studio/semstreams/natsclient"' -- 'internal/**/*.go' \| grep -v _test` | bootstrapobservability, e2eslowconsumer, graphmutation, maxdelivery |
| Test strings the package must not change | `git grep -n 'delivery work panicked\|ownership lost"\|refused by latched lane' -- 'processor/**/*_test.go'` | 12 assertions (governance `:138` pins `governance delivery work panicked`; dispatch `:219`/tools `:120` pin the refusal log lines) |
| Refusal declared on three lanes only | `git grep -n 'recordDeliveryRefused(' -- 'processor/agentic-*/component.go'` | dispatch `:611`, `:684`; tools `:435` |
| Rebased L3 head descends from this base | `git merge-base --is-ancestor 20fe8d09 c58c65bd; echo $?` | 0 |
| Five files byte-identical at the rebased L3 head | `git diff --stat 20fe8d09 c58c65bd -- 'processor/*/delivery_owner.go'` | empty |
| Dispatch lanes at the rebased L3 head | `git grep -nE 'newDeliveryLaneAdmission\(' c58c65bd -- 'processor/agentic-*/component.go'` | 8 constructions: dispatch `:569`, `:610`, `:651`; governance `:509`; loop `:1098`, `:1120`; model `:408`; tools `:434` |
| Deleted dispatch lanes | `git grep -n 'agentCreated\|approvalPending' c58c65bd -- processor/agentic-dispatch/component.go` | no hits (`2693df0e`) |
| Declarers at the rebased L3 head | `git grep -n 'recordDeliveryRefused(' c58c65bd -- 'processor/agentic-*/component.go'` | dispatch `:611`, `:652`; tools `:435` → 3 of 8 declare, 5 silent (#1342) |
| Eleven drain-once planes, one drainer each | `for f in <eleven>; do grep -cE '\.Drain\(\)' $f; done` | 1, eleven times |
| Test census | `git grep -lE '<census regex>' -- 'processor/agentic-*/*_test.go'` + awk to `func Test` | 36 tests / 16 files / 4 integration-tagged (superseded below) |
| **— round 3, base `3faca84f` —** | | |
| Five files still present and byte-identical | `git ls-tree -r --name-only HEAD \| grep delivery_owner.go`; `git diff --stat 20fe8d09 3faca84f -- 'processor/*/delivery_owner.go'` | five files; empty diff |
| L3 did not touch them | `git diff --stat fbd3c173 3faca84f -- 'processor/*/delivery_owner.go'` | empty |
| Still 550 lines | `wc -l processor/agentic-{dispatch,tools,loop,model,governance}/delivery_owner.go` | 136, 122, 116, 101, 75 = 550 |
| Lane constructions | `git grep -nE 'newDeliveryLaneAdmission\(' -- 'processor/agentic-*/component.go'` | 8: dispatch `:577`, `:618`, `:659`; governance `:509`; loop `:1098`, `:1120`; model `:408`; tools `:434` |
| Deleted dispatch lanes stay deleted | `git grep -n 'agentCreated\|approvalPending' HEAD -- processor/agentic-dispatch/component.go` | no hits |
| Declarers | `git grep -n 'recordDeliveryRefused(' -- 'processor/agentic-*/component.go'` | dispatch `:619`, `:660`; tools `:435` → 3 of 8 declare, 5 silent (#1342) |
| Test strings the package must not change | `git grep -n 'delivery work panicked\|ownership lost"\|refused by latched lane' -- 'processor/**/*_test.go'` | 15 assertions (governance `:138` pins `governance delivery work panicked`; dispatch `:248` / tools `:120` pin the refusal log lines) |
| Test census, regenerated | same command + attribution to `func Test` | **45 tests / 23 files / 5 integration-tagged** |
| Pin re-derivation | `repin.py` (difflib opcodes, `sed -n "${n}p"`), then `scripts/inventory-verify.sh` | 178 same, 75 moved, 7 remapped to the L1 archive, 9 gone, 0 text changes; `pins=272 ok=272 … EXIT=0` |

## Searches

Zero-hit and closing searches, verbatim, stderr visible (none redirected).

- `git grep -n -i 'deliveryLaneAdmission\|laneadmission\|deliverylane' -- . ':!processor/agentic-*' ':!openspec' ':!docs'` → exit 1, no hits (no shared spelling outside the five components)
- `ls -d internal/deliverylane pkg/deliverylane internal/laneadmission` → all "No such file or directory"
- `git grep -n -E 'func .*[Aa]dmit\(' -- '*.go' ':!*_test.go'` → the five `admit()`, `pkg/fusion/contract.go:331` (budget), `agentic-tools/executors/graph_query.go:855` (walk dedupe) — no latch elsewhere
- `git grep -n 'drainOnce' -- '*.go' ':!*_test.go' \| grep -v delivery_owner.go` → `natsclient/client.go:757,788`, the five binding structs, governance `component.go:729-732`
- `git grep -n 'OwnerStopRequired()' -- '*.go' ':!*_test.go' \| grep -v natsclient/delivery_settlement.go` → 17 hits, all in the five components (§ 4)
- `git grep -n 'natsclient\.ConsumeDeliveryWithHeartbeat\|natsclient\.SettleDelivery' -- '*.go' ':!*_test.go' ':!natsclient/'` → the five components + `component/port_jetstream.go:43` (comment)
- `git grep -n -i 'owns no lifecycle\|no shared helper\|shared helper owns' -- . ':!openspec/changes/archive'` → jcp `:651`, L1 delta `:21`, `:51`
- `git grep -n 'deliveryFatalErr' -- gateway/ service/ health/` → no hits
- sister probe, read-only, no go commands: `for s in semsource semteams semdev semconnect semboids semmachina semlink semops semdragon semmem; do git -C /Users/coby/Code/c360/$s grep -n 'ConsumeDeliveryWithHeartbeat\|OwnerStopRequired\|SettleDelivery\|ConsumeWithHeartbeat\|natsclient\.DeliveryResult\|HeartbeatDeliveryPolicy' -- '*.go'; done` → only semdev (`ca3956a`): `internal/conversationchannel/component.go:476`, `internal/intake/component.go:378` (legacy `ConsumeWithHeartbeat`), plus three comments
- `gh pr view 1329/1330/1249/1327/1328` → no PR with those numbers; `gh pr list --state open --limit 50` → #1334 (L1, head `20fe8d09`), #1335 (L2), #1338 (L3, head `f4fd4369`), #1339 (L0.5); no PR claims #1341
- `git ls-remote origin refs/heads/claude/gh1327-settle-after-effect` → `20fe8d09` (this base is the pushed L1 head)
