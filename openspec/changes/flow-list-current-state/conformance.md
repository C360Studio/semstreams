# Conformance — flow-list-current-state

Per-ruling map from the owner-accepted flow-CRUD design (`docs/proposals/gh1008-1010-flow-crud-design.md:9-11`
acceptance; `:293-304` the twelve rulings; `:104-124` the Slice B contract) to the code, spec delta, and test that
carry it. Every `file:line` was re-measured at `01103ce3`, the head that holds the last change to any `.go` file or
spec delta on this branch; `tasks.md` rows cite section numbers, which do not shift.

| # | Ruling / contract bullet | Implementation | Spec delta | Test / evidence |
|---|---|---|---|---|
| R1 | Option 2A and four pre-tag slices (`:293`) | Slice B is this change: `openspec/changes/flow-list-current-state/proposal.md:3-4` | `specs/flow-authoring/spec.md:3` | draft PR #1085, `Closes #1010` |
| R2 | CAS repair inside #1009 (`:294`) | N/A — Slice A: CAS repair belongs to #1009 | — | — |
| R3 | Copy-on-write, success-only caller mutation (`:295`) | N/A — Slice A: copy-on-write governs Update | — | — |
| R4 | Stable issue types, aggregate structural validation (`:296`) | N/A — Slice C: validation vocabulary lands in #1008 | — | — |
| R5 | HTTP 200 for well-formed invalid drafts (`:297`) | N/A — Slice C: invalid-draft status lands in #1008 | — | — |
| R6 | `connection_pattern_error` as validation finding (`:298`) | N/A — Slice C: validation findings land in #1008 | — | — |
| R7 | Flow error response without a machine-code field (`:299`) | N/A — Slice C: Flow error schema is #1008 | — | — |
| R8 | Must-exist DELETE (`:300`) | N/A — Slice C: must-exist DELETE lands in #1008 | — | — |
| R9 | Request-schema separation and legacy bodies (`:301`) | N/A — Slice C: request schemas land in #1008 | — | — |
| R10 | Separately reversible pre-tag Get projection (`:302`) | N/A — Slice D: Get projection lands in #1060 | — | — |
| R11 | Mandatory semstreams-ui candidate validation (`:303`) | → #1087 checklist (downstream gate) — sister repo is hands-off; its owner runs it against the combined candidate and the 9/9 evidence is recorded on #1087 | — | not satisfiable inside this PR |
| R12 | Named E2E scenarios and assertion totals (`:304`, `:275-277`) | → #1087. **Filing #1087 records the obligation; it does NOT complete the pre-tag guarantee.** The two Slice-B-relevant scenarios (`flow-list-current-state` core, `flow-crud-tools-empty` crud-tools) are unauthored and their baseline-failure counts are measured against design baseline `774c85dc`, which only the combined proof re-runs | — | the per-slice gate this PR ran is a separate obligation, recorded at `tasks.md` 6.1-6.6 (lint, full `-race` unit + integration, build, schema no-drift, contract) and 6.7 (this correction's gates); the List assertion total the design names (`:119`) is the row below |
| B1 | Empty bucket → successful non-nil empty `[]*Flow` (`:104`) | `flowstore/manager.go:252` (`s.kvStore.Keys`), `:263` (`make([]*Flow, 0, len(keys))`), `:278` | `specs/flow-authoring/spec.md:17-23` | `flowstore/manager_integration_test.go:488` |
| B2 | Key deleted between enumeration/Get → omitted only on typed absence (`:105`) | `flowstore/manager.go:270-272` (`errors.Is(err, natsclient.ErrKVKeyNotFound)` → `continue`) | `specs/flow-authoring/spec.md:25-34` | `flowstore/manager_integration_test.go:503` |
| B3 | Transport, permission, deadline, corrupt JSON, and other failures abort (`:106`) — **including enumeration-time cancellation** | `flowstore/manager.go:273` (per-key abort, plain `%w` so `Manager.Get`'s class survives); `:259-261` (the enumeration-time guard, added for the Codex BLOCKING); `:49` (`afterListKeys`, the package-private seam it is proven through) | `specs/flow-authoring/spec.md:36-44`, `:46-52`, `:54-65` (`cancellation during enumeration aborts`), and the requirement sentence at `:12-14` | `flowstore/manager_integration_test.go:548`, `:585`, `:625` (`TestManagerListRejectsCancellationDuringEnumeration`, subtests at `:645` and `:663`) |
| B4 | No message substring anywhere on the List path (`:107`) | `service/flow_service.go:126-130` (startup: plain `if err != nil`), `:267-274` (`handleListFlows`: opaque 500, no text branch), `processor/agentic-tools/executors/flows.go:178-185` (tool: `len(flows) == 0`, no text branch). `grep -rn "no keys found" service/flow_service.go processor/agentic-tools/executors/flows.go flowstore/manager.go` → 0 hits at `01103ce3` | `specs/flow-authoring/spec.md:68-70` | `tasks.md` §4.3 (M3 — the empty bucket errors again with the old text and all four consumer tests MUST fail, which is what proves they branch on the typed outcome) |
| B5 | No new ordering promise (`:107`) | `flowstore/manager.go:250` (doc comment: "List promises none"); no sort anywhere in `List` (`:251-279`) | `specs/flow-authoring/spec.md:15` | `flowstore/manager_integration_test.go:480-486` (`listIDs` compares single elements and sets, never positions) |
| B6 | HTTP `{"flows":[]}` present/non-null (`:108`) | `service/flow_service.go:289-295` (`newFlowListResponse` always allocates), `:282-284` (`FlowListResponse`, value elements) | `specs/flow-authoring/spec.md:76-83` | `service/flow_service_test.go:484` |
| B7 | Startup import accepts empty (`:108`) | `service/flow_service.go:126-130` | `specs/flow-authoring/spec.md:85-91` | `service/flow_service_test.go:506` |
| B8 | Real FlowExecutor returns the exact empty content with no error attachment (`:108-109`) | `processor/agentic-tools/executors/flows.go:183-185` (unchanged code; the branch became reachable) | `specs/flow-authoring/spec.md:93-99` | `processor/agentic-tools/executors/flows_integration_test.go:23` (exact `==` at `:42`) |
| B9 | `FlowListResponse` with required non-null `flows: Flow[]` (`:123`) | `service/flow_service.go:282-284`, `:245` (`ResponseTypes`), `:221` (`GET /flows` `200` `SchemaRef`); generated at `specs/openapi.v3.yaml:125` and `:1449` | `specs/flow-authoring/spec.md:101-115` | `service/flow_surface_test.go:43` (list assertions at `:76-121`) |
| B10 | The seven named tests are present (`:114-117`) | — | every scenario in `specs/flow-authoring/spec.md` names its test | `flowstore/manager_integration_test.go:488`, `:503`, `:548`, `:585`; `processor/agentic-tools/executors/flows_integration_test.go:23`; `service/flow_service_test.go:484`, `:506` — 7/7, plus the eighth added for the Codex BLOCKING at `flowstore/manager_integration_test.go:625` |
| B11 | At least 14 assertions (`:119`) | — | — | **79 assertion statements** measured at `01103ce3` (`t.Error*` / `t.Fatal*` / `assert.` / `require.` inside each test body, fixture guards included): 35 across the five `flowstore` List tests, 7 in the tool test, 20 in the two `service` empty tests, 10 in the `flow_surface_test.go` list block (`:76-121`), 7 in the populated-list block (`service/flow_service_test.go:135-146`). Behaviour-only in `flowstore` alone (setup guards excluded) is 27 |
| B12 | Baseline RED captured for real empty Manager/tool and vanished key (`:120`) | — | — | `tasks.md` §2.6 — verbatim `--- FAIL` lines at `a2f7620d` + the tests + the 2.1 seam only |
| B13 | Forced omission must fail the core regressions (`:120-121`) | — | — | `tasks.md` §4 — M1 (4.1), M2 (4.2), M3 (4.3), M4a/M4b (4.4), M5 (4.5), M6 (4.6), M7 (4.8, the enumeration guard); post-restore `shasum` equality recorded with each |
| **DEVIATION** | Design text (`:109`) quotes the tool's empty content as `No flows configured` without a period; the code emits and this change pins `No flows configured.` **with** the period (`processor/agentic-tools/executors/flows.go:184`, `specs/flow-authoring/spec.md:98`, `processor/agentic-tools/executors/flows_integration_test.go:42`) | Owner-signed: keep the literal with the period — https://github.com/C360Studio/semstreams/pull/1085#issuecomment-5425495552 (cglusky, 2026-08-26, "keep the `No flows configured.` literal with the period, as the delta pins it") | `specs/flow-authoring/spec.md:96-98` | `processor/agentic-tools/executors/flows_integration_test.go:42` |
