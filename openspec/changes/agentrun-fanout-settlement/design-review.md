# #1249 pre-owner design review (round 2) — reviewer

Mode: pre-owner design review, read-only. Base 0053183d, commands from `semstreams-wt/verify-0053183d` unless a `git -C` path is named. Not owner approval.
`./scripts/inventory-verify.sh <inventory>` → `changed since base (0053183d..HEAD):` / `  (none)` / `pins=146 ok=146 moved=0 ambiguous=0 drift=0 malformed=0 unparsed=0`, exit 0. No non-OK line to quote.
Round-1 pass: all 8 findings are closed in the inventory, not merely acknowledged (governance as fifth same-class owner; `:641`; `:646-648`; the `milestoneStarter` seam + three `Health()` sites + five latch owners; spec `:329`/`:333` + delta `:89`; the 12/4 counts; the stranded `PermanentDeliveryError` doc; the third ratchet family). Seven are carried into the design; the eighth (`consume_durable.go:39` on main) is now in § 4.

## BLOCKING

B1 § 2.6 / § 6.4 / Q2 - the only named signal for a stuck milestone can be neither built nor registered
- Mechanism (a): § 2.6 observes the histogram "with `attempt.Number()`". At L0's head (`git -C .../gh759-semantic-settlement`, `29bd7dae`) `DeliveryAttempt` has no Go source reference left and `DeliveryWork` is `func(context.Context, []byte) (DeliveryDecision, error)` (`natsclient/delivery_settlement.go:35`); commit `266453eb` is "withdraw the zero-consumer delivery attempt" and L0 `design.md:309` lists re-exposing the count as a rejected alternative. Work cannot see the attempt.
- Mechanism (b): § 2.6 routes both metrics through `milestoneStarter.RegisterMetrics` delegated by `MilestoneService`. Nothing calls `Service.RegisterMetrics`: whole-tree grep returns only `service/base.go:389` (decl), `:494` (interface member), one impl, and tests. The repo states it at `service/storage_observability.go:246` — "nothing in the framework calls that method today — a metric registered only there would never reach the registry the /metrics endpoint scrapes, which is the phantom-signal class this capability exists to remove."
- Fix: register at construction from the composition root as storage-observability does; smallest Tier-1-safe shape is an additive `MilestoneSubscriber.RegisterMetrics(metric.MetricsRegistrar) error` called at `cmd/semstreams/main.go:347` and `cmd/e2e-semstreams/main.go:272` (`metricsRegistry` already in scope, `main.go:314`). A constructor parameter would break Tier 1. Then re-derive what the attempt label can be.
- Refutation attempted: searched for any manager-side invocation and for an attempt accessor on `DeliveryResult` at L0 head — neither exists; `NumDelivered` is read at `:306` and discarded.

B2 § 2.3 rows for `:637` and `:657` - deterministic errors classified transient, retried unboundedly
- Mechanism: both rows read "error other than NotFound → transient → Retry". `Manager.Get` has a second deterministic not-found sibling, `ErrEntityNotLifecycleManaged` (`pkg/lifecycle/errors.go:36-43`, returned `manager.go:247`) — "exists but no phase triple", the ADR-049 Q5 forward-reference case, i.e. exactly a non-run loop. `getWithRevision` also returns `lookupByWorkflow` and `projectTriples` errors (`:236`, `:251`), and `ResolveRun` wraps entity-ID grammar failures into `:657` (`agentrun.go:393`, `:405`, `:437`). All invalid-class. `git grep -n 'ErrEntityNotLifecycleManaged' -- agentic/ processor/ service/` → no output, exit 1: nothing handles it. Today all are swallowed to a nil run and ACKed; under the design plus Q2's `MaxDeliver 0` they replay forever, re-invoking every handler's side effects each attempt.
- Fix: classify by `pkg/errs` class or an explicit sibling set (`ErrEntityNotFound` OR `ErrEntityNotLifecycleManaged` → continue with nil run; grammar/type/projection → Terminate), and bound the residual "other" row.
- Refutation attempted: checked whether tests pin the fast path — `agentrun_test.go:730` uses an empty `RunEntityID`, so it exercises `:653`, not `:637`. The fast-path classes are untested and unguarded.

B3 § 2.7 / Q2 - `MaxDeliver 0` silently disables an existing framework-guaranteed signal
- Mechanism: the inventory's "the fifth dropped with no in-tree observer" came from a search scoped to `natsclient agentic`, which cannot see `internal/maxdelivery/observer.go`. That observer subscribes `$JS.EVENT.ADVISORY.CONSUMER.MAX_DELIVERIES.>` and registers `max_delivery_exhaustions_total` per stream/consumer (`observer.go:36`, `:132`, `:148`), is started in the framework binary at `cmd/semstreams/main.go:232`, and is backed by a fixed framework stream whose comment says changing it "could silently remove the failure signal the framework is promising to expose" (`config/streams.go:184-196`). JetStream emits that advisory only on exhaustion of a finite `MaxDeliver`; moving the two milestone lanes from 5 to 0 makes the counter unable to fire for `agentrun-milestone-complete`/`-failed`. The design trades a live registered signal for the one B1 shows does not exist.
- Fix: price Q2 against this owner — keep a finite `MaxDeliver` and cite `max_delivery_exhaustions_total`, or state that the owner is being asked to disable it, and why.
- Refutation attempted: checked whether the observer is component-scoped or opt-in — neither; it is per-consumer and framework-wired in phase-A bootstrap.

## HIGH

H1 § 4 spec deltas - both removals are already claimed by L0; the real debt is the opposite one
- Mechanism: § 4 plans REMOVED blocks for `jetstream-consumer-policy` "Durable settlement composition is stateless" and `nats-streaming` "Heartbeat consumption SHALL expose settlement failure". L0's delta already REMOVES both, at base (`.../jetstream-consumer-policy/spec.md:335`, `.../nats-streaming/spec.md:79`) and at L0 head (`:342`, `:81`). After L0 archives neither exists to remove. What L0 head *adds* and #1249 must retire is the inverse text: consumer-policy "`ConsumeWithHeartbeat` SHALL have no alias and no new production caller, and SHALL be deleted by the PR that migrates its last one (#1249)" with scenario "carries only its ratcheted remaining callers", plus the whole nats-streaming requirement "the legacy helper is a shrinking remainder, never a compatibility surface" and its scenario. Both need MODIFIED/REMOVED blocks restating every scenario. None of this is in § 4.
- Cross-layer interval, as asked: on L0's actual head the absence half is already fixed — `29bd7dae` "docs(openspec): remove the deprecation, not the greenfield cutover" rewrote `:78`/`:89` to assert the helper still exists, so option 1 of the two § 4 offers the coordinator is already taken and that bullet is stale. A narrower interval problem survives: L0 still REMOVES the two requirements governing the still-present helper's settlement behavior, and its nats-streaming Reason calls it "the removed `ConsumeWithHeartbeat` export" while L0 leaves it in place.

H2 § 4 migration text - written to a home L0's own requirement contradicts
- L0 head's nats-streaming requirement states "Adopters get the removal from `docs/operations/migration-beta162-to-beta163.md`, not from a deprecation marker. (Owner ruling 2026-09-18.)" § 4 writes only into `migration-restart-safe-nats-client.md:95-97`. `grep -n -i 'ConsumeWithHeartbeat\|heartbeat' docs/operations/migration-beta162-to-beta163.md` → no output, exit 1. Fix: write the removal into the named doc too, or have the coordinator relay that the L0 sentence is wrong.

H3 § 2.3 - no row for the control-plane fatals the typed API produces with no handler involved
- `ConsumeDeliveryWithHeartbeat` sets `ownerStopNeeded = true` on an `InProgress()` failure (`natsclient/delivery_settlement.go:361-368`) and returns Quarantine + owner stop when metadata is unavailable (`unavailableDeliveryMetadata` `:379-386`, reached `:323`/`:326`/`:330`). Both stop the milestone lane and, with § 2.6's override, mark the service unhealthy. Today the same InProgress failure is a WARN with the lane still running (`heartbeat.go:113`). An operator-visible behavior change the matrix does not enumerate and the adopter seam does not mention.

H4 § 2.5 fallback - "byte-for-byte" is unachievable, and the copyable half collides with an existing owner
- Against the now-authoritative fallback (L1 landed `f420128a`; governance got its own `processor/agentic-governance/delivery_owner.go` at `1b8598ce`): the cited range `processor/agentic-loop/delivery_owner.go:26-97` is not receiver-free. `:64` is `func (c *Component) recordDeliveryOwnerFatal` and `:88` `newStreamConsumerBinding` returns the component-local `streamConsumerBinding`. Only `:29-62` (`deliveryLaneAdmission`, its constructor, `admit`, `latch`) and `:73-85` `consumeAdmittedDelivery` copy verbatim. The handle half is the problem: AgentRun already has `milestoneConsumerOwner` (`agentrun.go:679-756`) owning both handles, `completeDrained`/`failedDrained`, the Closed waits, and the ordering invariant at `:718` ("Both running handles begin Drain before either exact Closed wait"). A copied `streamConsumerBinding` with its own `drainOnce` is a second owner of the same `jetstream.ConsumeContext`; an observer calling `binding.drain()` on a fatal drains a lane whose drained flag is still false, so a later `stop()` re-Drains and waits on `Closed()` with the both-drain-first ordering broken.
- What the fallback text is missing: that only the admission half is copied verbatim, that `milestoneConsumerOwner` stays the sole handle/drain owner, and how the observer marks a lane drained through it. The residual-not-an-issue framing is right and is not the gap.

## MEDIUM

M1 § 2.5's "This layer adds NO seventh private copy" is stale under the fallback: `git -C .../gh1327-settle-after-effect grep -ln 'deliveryLaneAdmission' HEAD -- '*.go' ':!**/*_test.go'` → dispatch, governance, loop, model, tools. AgentRun's would be the sixth.

M2 § 6 is honest about rank, not about the trade. It records the idempotence obligation as an OPEN residual at seam rank doc/test — correct, and consistent with the adopter-seam rule that a correctness fact discoverable only from documentation is a finding. It does not state the direction of the change: today a handler ignoring identity loses a transient failure silently (at-most-once); under C the same naive handler produces duplicate durable effects on every replay (at-least-once). C converts silent loss into silent duplication for the do-nothing adopter, and the framework can detect neither. Nor does § 6 state that one permanently failing handler blocks Ack for all N and re-runs every other handler's effects each attempt (I1 + the aggregate priority) — which is the actual case for per-handler settlement, an option A–E never frames. § 6.3 answers a narrower question ("handlers that never ran"). The strongest case against C is that N handlers share one failure domain with no isolation and no framework-verifiable obligation; the design should say so and let the owner weigh it rather than leaving the option space at D (receipts) and E (arity-1), both easy to reject.

M3 Pins the layers below moved. § 2.4 W3 cites `delivery_settlement.go:268` `ServerConfirmed()`, withdrawn at L0 head (`tasks.md:28`; zero Go hits). § 4's "the `Deprecated:` notice (`heartbeat.go:75`; still present at both PR heads)" — present at base and at L1 `f420128a`, already absent at L0 `29bd7dae`. Neither changes a conclusion; both are task lines that will not apply at the rebase base.

M4 § 7 size. Additions credible; removals close (heartbeat.go is 151 lines retaining ~35, so natsclient production is ≈ −116 not −155; `heartbeat_test.go` 407 + `heartbeat_integration_test.go` 133 = 540 not ≈420 — the errors roughly cancel at −650). "~17 files" is low: the change dir (6), spec sync (3), both mains, the e2e scenario + stage test, three docs, the ratchet, two foreign test comments, and the agentrun/service test files reach ≈25-30. Still 3x inside the ~100-file breaker, so the conclusion holds; correct the count so the margin is honest.

## NIT

N1 `interpretDeliveryWork` (`delivery_settlement.go:387-408`) requires cause == nil for Ack and cause != nil for Retry/Terminate/Quarantine, else it forces Quarantine + owner stop. The § 2.3 identity-guard row (`:646-647`, a branch with no error today) must synthesize a non-nil cause or it latches the lane.

## Verified, refutation failed — not findings

- Claim A holds: `HandleEvent` consumes handler errors (`:612-617`) and panics (`:604-610`), returns nil at `:621`, the closure returns nil, `heartbeat.go:140` Acks. A 1-of-n fanout ACKs.
- Removal enumeration is right at the real rebase base: at L1 head `f420128a`, `git -C .../gh1327-settle-after-effect grep -n 'ConsumeWithHeartbeat' HEAD -- '*.go' ':!**/*_test.go'` → `agentic/agentrun/agentrun.go:812` plus three comment/decl lines in `heartbeat.go`; `consume_durable.go` absent. § 4's re-run-at-rebase instruction and its stop-if-anything-else guard are the correct shape.
- The ratchet inversion is base-independent: both families are `reflect.DeepEqual` against exact maps (`consumer_policy_callsite_test.go:426`, `:443-447`) and the surface-guard corpus is synthetic `t.TempDir()` source (`:455`, `:476`), so nothing depends on the caller set at the base.
- Retaining `ErrHeartbeatFailed`/`PermanentDeliveryError`/`TerminateDelivery` is correct, not phantom: live consumers at L1 head at `processor/agentic-loop/component.go:1198`, `:1293`, `processor/agentic-tools/component.go:484`, `:515`, and many `TerminateDelivery` sites.
- I3 holds: a Quarantine decision returns early from `settleDeliveryDecision` before any terminal method.
- § 2.8 holds: the typed API's `ctx.Done()` branch settles on the joined work decision; no legacy 5s NAK.
- Tier 1 (`release/tier1-packages.txt`): `agentic/agentrun` line 40, `natsclient` line 57. Both new exported additions (`LoopTerminalEvent.SourceMessageID`, `MilestoneSubscriber.DeliveryFatal()`) are additive and nothing else changes exported shape — except that removing `natsclient.ConsumeWithHeartbeat` is itself a Tier 1 incompatible removal, and the design never names `scripts/api-compat.sh` / ADR-106 as a gate it must pass (report-mode today, but the removal should be declared).

DESIGN CHANGES REQUESTED
